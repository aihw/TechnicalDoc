# 个人量化研究平台 · 开源项目调研与架构选型报告

> 版本：v1.0 ｜ 完成日期：2026-09-14 ｜ 面向对象：个人（非机构）长期自用量化研究实验室
> 数据快照：2026-09-14（Asia/Shanghai），Star/活跃度等均为当日抓取值
> 调研方式：直接抓取各项目仓库元数据、LICENSE 原文、README、PyPI 元数据，并**下载源码包实际检查目录结构与关键模块源码**（VectorBT v1.1.0、Qlib main、Backtrader、Backtesting.py、PyBroker、Hikyuu、ZVT、QUANTAXIS、QuantsLab、QuantStats、Alphalens-reloaded、NautilusTrader 等 15 个仓库源码包）
> 原始证据留存：`_research/raw/signals.json`（全部指标原始数据）、`_research/raw/files/*`（README/LICENSE 原文）、`_research/repos/*`（已解压源码）

---

## 1. Executive Summary

### 1.1 一句话结论

**不要自己写回测引擎，也不要自己写图表库。** 用 **Qlib（MIT）做研究流水线主干 + VectorBT 做参数扫描与快速实验引擎 + Parquet/DuckDB 做自己的数据仓库 + Plotly/QuantStats/Streamlit 做报告 + MLflow 做实验管理 + MCP 化的 AI 研究助手**。自己开发的只有 5 样东西，全部是"别人给不了、只有你能积累"的资产（见 1.3）。

### 1.2 明确推荐（不模棱两可）

| 决策项 | 结论 | 理由（关键证据） |
| --- | --- | --- |
| 回测引擎 | **不自己写**。Qlib（组合/ML/多因子）+ VectorBT（参数扫描）双引擎 | 两者源码均已具备完整撮合/成本/记录/统计能力，自研等于重新踩坑 |
| 研究主干 | **Qlib** 的 Workflow + Recorder + Backtest 作为骨架 | MIT 许可最干净、A股规则内建（涨跌停/停牌/100股/成本）、实验记录基于 MLflow 成熟 |
| 参数扫描 | **VectorBT** | `Portfolio.from_signals` 单函数 50+ 参数、Numba+Rust 加速、原生支持参数热力图与 walk-forward splitter |
| 数据仓库 | **Parquet（分区）+ DuckDB + SQLite**，不要 PostgreSQL/ClickHouse | 个人规模（3000 只 × 10 年日线 ≈ 千万行级）Parquet 压缩后仅数百 MB |
| 可视化 | **不自己写**。Plotly 出图 + QuantStats/Qlib/Alphalens 出指标 + Streamlit 组装报告 | 净值/回撤/买卖点/IC/分组收益/参数热力图全部已有现成实现 |
| 实验管理 | **MLflow**（Qlib Recorder 底层就是它） | 避免同时维护两套实验元数据 |
| 事件驱动/模拟盘 | **暂不引入 Backtrader**（2023-04 起停更、GPL-3.0）；需要时用 vnpy 或券商 QMT/Ptrade | Backtrader 0 commits/year，无国内实盘通道 |
| AI | **放在"研究意图层 + 编排层"**，用 MCP 工具暴露平台能力，禁止直接下单 | 参考 pybroker 的 `skills/`+`CLAUDE.md` 模式与微软 RD-Agent |

### 1.3 你的核心资产（只有这些值得自己写）

1. **A股数据仓库与数据管道**（采集/清洗/复权/停牌/财务 PIT/行业分类）——这是最耗时、最无可替代的部分。
2. **A股交易规则适配层**（T+1、涨跌停、停牌、最小 100 股、印花税/佣金/过户费、ST、退市、复权）——所有回测共用。
3. **因子库与因子验证流水线**（因子定义 + IC/分层/换手/衰减/相关性 + 稳定性检验）。
4. **实验元数据规范与报告模板**（一次实验 = 一份可复现的配置 + 数据版本 + 结果）。
5. **AI 研究 Agent 的工具层**（把上面 4 项封装成 MCP 工具，让 LLM 能安全调用）。

**框架不是资产，数据与规则才是。**

### 1.4 最大的三个风险（必须先知道）

1. **许可证风险**：VectorBT 与 PyBroker 都是 **Apache-2.0 + Commons Clause**（禁止"Sell the Software"，非 OSI 认可的开源许可）；Backtrader 是 **GPL-3.0**；Backtesting.py 是 **AGPL-3.0**；NautilusTrader 是 **LGPL-3.0**。个人自用没问题，但若将来要对外提供收费服务/产品，需重新评估（详见附录 A）。
2. **维护风险**：Backtrader 已停更（最后提交 2023-04-19，近一年 0 提交）；Qlib 最新 release v0.9.7（2025-08），近一年仅 26 次提交，处于"稳定但低活跃"状态；Tushare 开源仓库自 2020 年起停更（数据服务已商业化）。
3. **回测失真风险**：VectorBT 不做 A股规则（涨跌停/停牌/T+1/100 股），Qlib 做了但被封装在 `qlib/backtest/exchange.py` 里。**规则适配层必须自己写，且两个引擎必须共用同一套规则与成本假设**，否则参数扫描结果和最终回测结果会对不上。

### 1.5 关于 "quant-lab" 的重要说明

GitHub 上**没有一个公认的、与 Qlib/VectorBT 同量级的名为 `quant-lab` 的项目**。检索结果中最接近的是 `hummingbot/quants-lab`（297★，Apache-2.0，**加密货币/Hummingbot 生态**）。因此本报告第 6 节按"最可能被指代"的 hummingbot/quants-lab 做了深度分析，并列出全部候选（见 6.1）。如果你指的是另一个仓库，请告知，我可以针对该仓库重做分析。

---

## 2. 开源项目横向对比

### 2.1 图例说明

- `●` 原生完整支持 ｜ `◐` 支持但需额外封装/配置 ｜ `○` 不支持或需完全自研
- "活跃度"= 近 12 个月提交数 / 最后一次提交日期（2026-09-14 抓取）
- 星标数是**知名度**不是**适配度**；请重点看 License 与 A股 两列

### 2.2 主对比表

| 项目 | 定位 | 回测 | 因子 | 参数扫描 | 可视化 | 模拟盘 | 实盘 | A股 | 活跃度 | License |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **microsoft/qlib** | AI 量化研究全流程平台 | ● | ● | ◐ | ● | ◐ | ○ | ◐（有A股规则与cn数据采集器） | 49k★｜26/年｜2026-07-23 | **MIT** |
| **polakowo/vectorbt** | 向量化回测/参数扫描引擎 | ● | ◐（指标类强，财务因子自备） | ● | ● | ○ | ○ | ○（无A股规则） | 9.1k★｜147/年｜2026-08-02 | **Apache-2.0 + Commons Clause** |
| **mementum/backtrader** | 事件驱动回测框架 | ● | ○ | ◐ | ● | ◐ | ◐（IB/Oanda/VC，无国内） | ○ | 23k★｜**0/年｜2023-04-19（停更）** | **GPL-3.0** |
| **hummingbot/quants-lab** | 加密货币量化研究框架（Hummingbot 生态） | ◐ | ◐ | ◐ | ◐ | ◐ | ◐（Hummingbot 交易所） | ○ | 297★（小众，活跃） | Apache-2.0 |
| nautechsystems/nautilus_trader | 高性能事件驱动回测→实盘 | ● | ○ | ◐ | ◐ | ● | ●（加密/IB 等） | ○ | 29k★｜5.4k/年｜2026-09-14 | LGPL-3.0 |
| kernc/backtesting.py | 轻量事件驱动回测 | ● | ○ | ● | ● | ○ | ○ | ○ | 9k★｜33/年｜2026-08-05 | **AGPL-3.0** |
| QuantConnect/Lean | 机构级多资产引擎（C#/Python） | ● | ◐ | ◐ | ● | ● | ●（海外券商） | ○ | 22k★｜427/年｜2026-09-11 | Apache-2.0 |
| edtechre/pybroker | ML 友好回测 + **自带 AI Agent skills** | ● | ◐ | ● | ◐ | ○ | ○ | ◐（数据源需自备） | 3.5k★｜448/年｜2026-08-28 | **Apache-2.0 + Commons Clause** |
| stefan-jansen/zipline-reloaded | 事件驱动回测（Quantopian 血统） | ● | ◐ | ◐ | ◐ | ○ | ○ | ○ | 1.9k★｜4/年｜2025-11-13 | Apache-2.0 |
| pmorissette/bt | 组合/权重再平衡回测 | ● | ○ | ◐ | ◐ | ○ | ○ | ○ | 3k★｜171/年｜2026-09-12 | MIT |
| **fasiondog/hikyuu** | A股超高速量化框架（C++/Python） | ● | ◐ | ◐ | ● | ◐ | ◐（QMT 等合规终端） | **●（深度适配）** | 3.5k★｜1.3k/年｜2026-09-11 | Apache-2.0 |
| **yutiansut/QUANTAXIS** | A股全流程（数据/因子/回测/Web/分布式） | ● | ● | ◐ | ● | ◐ | ◐ | ● | 11k★｜29/年｜2026-09-01（Web 端停更于2020） | MIT |
| **zvtvz/zvt** | A股数据+因子+ML+交易 | ● | ● | ◐ | ◐ | ◐ | ◐ | ● | 4.3k★｜12/年｜2026-07-01 | MIT |
| vnpy/vnpy | 国内实盘交易平台（CTP/QMT 等） | ◐ | ○ | ○ | ● | ● | ● | ●（期货/股票实盘生态） | 45k★｜54/年｜2026-08-06 | MIT |
| akfamily/akshare | A股/全球财经数据接口库 | — | — | — | — | — | — | ● | 23k★｜374/年｜2026-08-28 | MIT |
| waditu/tushare | A股数据 SDK（数据服务已商业化） | — | — | — | — | — | — | ● | 15k★｜**0/年｜2020-03-04** | BSD-3-Clause |
| stefan-jansen/alphalens-reloaded | 因子分析（IC/分层/换手） | ○ | ● | ○ | ● | ○ | ○ | ◐（需自备数据） | 646★｜0/年｜2025-06-02 | Apache-2.0 |
| ranaroussi/quantstats | 绩效分析/tearsheet 报告 | ◐ | ○ | ○ | ● | ○ | ○ | ◐ | 7.6k★｜13/年｜2026-01-13 | Apache-2.0 |
| duckdb/duckdb | 嵌入式分析型数据库 | — | — | — | — | — | — | — | 41k★｜23k/年｜2026-09-11 | MIT |
| pola-rs/polars | 列式 DataFrame 引擎 | — | — | — | — | — | — | — | 40k★｜2.3k/年｜2026-09-13 | MIT |
| apache/arrow | 列式内存/文件格式标准 | — | — | — | — | — | — | — | 17k★｜1.3k/年｜2026-09-12 | Apache-2.0 |
| mlflow/mlflow | 实验追踪/模型管理 | — | — | — | — | — | — | — | 28k★｜4.7k/年｜2026-09-14 | Apache-2.0 |
| iterative/dvc | 数据/模型版本管理 | — | — | — | — | — | — | — | 16k★｜84/年｜2026-08-06 | Apache-2.0 |
| streamlit/streamlit | 数据应用/报告 Web | — | — | — | ● | — | — | — | 46k★｜2.3k/年｜2026-09-12 | Apache-2.0 |
| plotly/plotly.py | 交互式图表库 | — | — | — | ● | — | — | — | 19k★｜671/年｜2026-09-11 | MIT |
| plotly/dash | 分析型 Web 仪表盘 | — | — | — | ● | — | — | — | 24k★｜1.3k/年｜2026-09-11 | MIT |
| jlowin/fastmcp | MCP 服务端框架（AI Agent 工具化） | — | — | — | — | — | — | — | 28k★｜1.8k/年｜2026-09-11 | Apache-2.0 |

### 2.3 许可风险一览（按"可直接商用"排序）

| 许可等级 | 项目 | 对个人自用 | 若将来商业化 |
| --- | --- | --- | --- |
| 宽松（MIT/Apache/BSD） | Qlib, bt, hikyuu, QUANTAXIS, zvt, vnpy, akshare, tushare, alphalens-reloaded, quantstats, duckdb, polars, arrow, mlflow, dvc, streamlit, plotly, dash, fastapi/fastmcp, Lean, zipline-reloaded | 无限制 | 无限制（保留版权声明） |
| 弱 Copyleft | NautilusTrader（LGPL-3.0） | 无限制 | 可商用，但修改 LGPL 部分需开源该部分，且需允许替换 |
| 强 Copyleft | Backtrader（GPL-3.0）、Backtesting.py（AGPL-3.0） | 无限制 | 传染性强；AGPL 连"网络提供服务"也触发开源义务，风险最高 |
| 非 OSI 开源（Open Core） | **VectorBT（Apache-2.0 + Commons Clause）**、**PyBroker（同上）** | 无限制 | **禁止"Sell the Software"**：不能把该软件本身（或价值主要来自它的产品/服务）收费出售 |

> ⚠️ Commons Clause 的原文（已核对仓库 LICENSE.md 原文）：*"the grant of rights under the License will not include, and the License does not grant to you, the right to Sell the Software."*
> 对"个人量化研究实验室"而言**完全够用**；但如果未来要做"卖出策略信号/托管服务/软件产品"，需要法务评估或购买商业授权（VectorBT PRO / PyBroker 商业版）。

---

## 3. VectorBT 深度分析

> 分析对象：`polakowo/vectorbt` master 分支 + **v1.1.0 tag 完整源码包**（54MB，含 `vectorbt/` Python 包 + `rust/` Rust 引擎）
> 源码规模：`vectorbt/portfolio/base.py` 247KB、`portfolio/nb.py` 290KB（Numba 内核）、`indicators/factory.py` 159KB、`generic/accessors.py` 107KB

### 3.1 项目现状（先看这里，因为它影响能不能长期依赖）

| 项 | 值 |
| --- | --- |
| Star / Fork / 贡献者 | 9.1k★ / 1.2k / 21 |
| 最近提交 | 2026-08-02（近一年 147 次提交） |
| 最新版本 | v1.1.0（PyPI 同步发布，63 个历史版本） |
| Python 版本 | **`>=3.11, <3.15`**（v1.1.0） |
| 关键依赖 | `numpy>=2.4.6`、`pandas>=3.0.3,<4.0`、`numba>=0.66`、`scipy`、`plotly`、`ipywidgets`、`scikit-learn`、`dill` |
| 加速方式 | Numba JIT（主）+ 可选 `vectorbt-rust`（PyO3 预编译内核，`pip install "vectorbt[rust]"`） |
| 许可 | **Apache-2.0 with Commons Clause**（v1.1.0 与 master 的 `LICENSE.md` 均为原文核对） |
| 商业定位 | 开源社区版（CE），作者主推闭源付费版 **VectorBT PRO** |

> ⚠️ 关键情报：项目 README 明确写着 "VectorBT is the open-source community edition of VectorBT PRO"。开源版仍在维护（bug 修复、数值稳定性、文档），但**重量级新特性（如 Triple Barrier 标注、更多数据源、高级可视化、实盘/通知）基本只进 PRO**。长期路线图上要预期"开源版稳定但特性增长缓慢"。

### 3.2 回测能力（逐项核对 `Portfolio.from_signals` 真实签名）

VectorBT 的 5 个组合构造入口：`from_signals`（信号驱动）、`from_orders`（订单驱动）、`from_order_func`（自定义逐 bar 撮合函数）、`from_holding`（买入持有基准）、`from_random_signals`（蒙特卡洛基线）。

| 你的需求 | 支持 | 实现方式与证据 |
| --- | --- | --- |
| 单股票回测 | ● | `vbt.Portfolio.from_signals(close=series, entries=..., exits=...)` |
| 多股票批量回测 | ● | 传入 `DataFrame`（列为股票代码），pandas 原生广播；`group_by` 控制分组、`cash_sharing=True` 支持资金共享组合 |
| 多策略 | ● | 策略只是信号矩阵，列 = 股票×策略；`pf.regroup()` 重排分组 |
| 多参数组合 | ● | 列索引升为 MultiIndex（如 `(symbol, fast_win, slow_win)`），一次算完全部组合；配合 `vbt` 参数广播 |
| 不同持有周期 | ● | 由 entries/exits 或 `labels` 模块（FIXLB/TRENDLB/LEXLB/BOLB 等）定义；持有期不是硬编码参数 |
| Entry/Exit | ● | `entries / exits / short_entries / short_exits`，或自定义 `signal_func_nb` |
| 手续费 | ● | `fees`（比例）、`fixed_fees`（固定额，可模拟 5 元最低佣金） |
| 滑点 | ● | `slippage`（比例滑点）；自研扩展可做 A股 涨跌停拒单 |
| 仓位管理 | ● | `size`、`size_type`（金额/数量/百分比）、`min_size`、`max_size`、`size_granularity`（可设为 100 股一手）、`accumulate`、`direction` |
| Stop Loss | ● | `sl_stop`、`sl_trail`（跟踪止损）、`stop_entry_price`、`stop_exit_price`、`adjust_sl_func_nb` |
| Take Profit | ● | `tp_stop`、`adjust_tp_func_nb`；SL/TP 可逐元素（逐股票逐参数）配置 |
| Benchmark | ● | `benchmark_returns` / `benchmark_value`，组合 stats 中直接输出 `benchmark_return` 与超额指标 |
| 拒单/成交概率 | ● | `reject_prob`、`allow_partial`、`upon_long_conflict`、`upon_opposite_entry`（可模拟涨跌停无法成交） |

**结论：VectorBT 的回测能力足以覆盖"信号型中低频策略"的全部需求；但它不是订单簿/撮合级引擎。** 它**没有** A股 专属规则（T+1、涨跌停、停牌、ST），这些必须自己写进信号矩阵或扩展 `from_order_func`。

### 3.3 因子研究能力

| 你列出的因子/需求 | 支持情况 |
| --- | --- |
| MA / RSI / MACD / 布林带 / ATR / OBV / STOCH | ● `vectorbt/indicators/basic.py` 原生提供：`MA, MACD, RSI, BBANDS, ATR, OBV, STOCH, MSTD` |
| 成交量 / 换手率 / 波动率 / 动量 | ● 成交量类指标原生（OBV、VWAP 等）；换手率/动量/波动率属于普通 pandas/numpy 表达式，`vbt` 的 `.vbt.rolling_*` 访问器可直接算 |
| 自定义因子（数百个） | ● `IndicatorFactory`（159KB）把任意 numpy 函数变成支持参数扫描、广播、缓存、绘图的"指标类"；也支持 TA-Lib / pandas-ta / `ta` 集成（`full` extra） |
| 财务因子（PE/PB/ROE/营收增速…） | ◐ **框架不提供数据**，但你只要把财务数据对齐成 `DataFrame`（index=日期，columns=股票），VectorBT 就能当普通列处理。真正的难点在**财务数据的 PIT（时点）对齐**，而这在 VectorBT 里没有现成支持，需要你在数据层解决 |
| 大量参数组合实验 | ● 这是 VectorBT 的**最强项**：参数即列，一次向量化算完，无需 Python 循环 |

**因子研究的标准用法**（VectorBT 官方范式）：
```
因子值 → 排序/分位 → 生成 entries/exits 信号 → Portfolio.from_signals(...) → 分组统计
```
若要严格的"因子有效性检验"（IC、ICIR、分层收益、换手率、因子衰减），建议**不要用 VectorBT 自己写**，直接用 `alphalens-reloaded`（见第 7 节），它是这个场景的成熟实现。

### 3.4 可视化能力（已实现清单，不要重复开发）

**组合级 `plot_*` 方法（源码逐个核对，位于 `vectorbt/portfolio/base.py`）：**

| 你的需求 | VectorBT 现成方法 |
| --- | --- |
| 净值曲线 | `plot_value()`、`plot_assets()`、`plot_cum_returns()`、`plot_cash()`、`plot_asset_value()` |
| 回撤曲线 | `plot_drawdowns()`、`plot_underwater()` |
| 买卖点 | `plot_orders()`、`plot_trades()`、`plot_positions()` |
| 交易分析 | `plot_trade_pnl()`、`plot_position_pnl()` |
| 现金流/暴露 | `plot_cash_flow()`、`plot_asset_flow()`、`plot_gross_exposure()`、`plot_net_exposure()` |
| 收益分布 | `vbt.returns` 的绘图访问器 + QuantStats 集成（`pf.qs`） |
| 参数热力图 | `df.vbt.heatmap()`、`df.vbt.ts_heatmap()`、`df.vbt.overlay_with_heatmap()`（对参数扫描结果直接出参数敏感性热力图） |
| 多策略比较 | 列广播天然支持；`pf.plot_value()` 一次画多条净值 |
| 交互式 | Plotly + Jupyter Widgets（`ipywidgets`/`anywidget`），文档中称为"Tableau 级"交互；也支持导出静态图（`imageio`、`kaleido`） |

**性能/统计指标（`vectorbt/returns/accessors.py` 实测存在）**：
`sharpe_ratio, sortino_ratio, calmar_ratio, omega_ratio, tail_ratio, information_ratio, deflated_sharpe_ratio, value_at_risk, cond_value_at_risk, annualized_volatility, max_drawdown`，以及全部对应的 **`rolling_*` 滚动版本**（滚动 Sharpe / 滚动波动率 / 滚动最大回撤 / 滚动 VaR…），正好覆盖你要求的 Rolling Return / Rolling Sharpe。

**QuantStats 集成**：`vectorbt/returns/qs_adapter.py` 把 QuantStats 的 60+ 指标与 tearsheet 图表挂到 `pf.qs` / `returns.qs` 上 → **"收益分布、月度热力图、滚动指标、tearsheet 报告"直接复用，无需自己写。**

### 3.5 性能分析：几千只 A股 × 5 年 × 数十因子 × 数百/数千参数组合，能不能扛住？

**先说数据量估算**（这是判断的核心）：

| 规模 | 计算 | 内存（float64） |
| --- | --- | --- |
| 3000 只 × 1250 交易日（5 年）单字段 | 3.75M 单元格 | **30 MB** |
| 同上，20 个字段（OHLCV+因子） | 75M 单元格 | 600 MB |
| **单参数组合**跑完整组合/交易记录 | 3.75M 订单/持仓 | 数百 MB～1GB |
| **1000 个参数组合同时展开** | 3.75M × 1000 | **≈30 TB（爆内存）** |

**结论：VectorBT 的瓶颈不是 CPU 而是"参数组合的笛卡尔积内存"。** 正确做法不是"一次算 1000 组 × 3000 只"，而是**分块**：

1. **按参数分批**：1000 个组合拆成 10 批 × 100 组合，每批约 3 TB→300 GB→继续拆；单批控制在可用内存的 50% 以内（16GB 内存建议单批 ≤ 500 万单元格/字段）。
2. **按股票分批**：3000 只拆成 10 批 × 300 只（A股 之间存在横截面依赖时注意分组边界）。
3. **降低单精度**：float32 直接减半（价格用 float32 在日线研究里精度足够）。
4. **只算必须的产物**：`Portfolio.from_signals` 的 `max_orders`、`max_logs`、`init_temp_records=False` 可显著降低内存；不需要逐笔记录时关掉记录。
5. **Numba 编译开销**：首次调用有 10~60s JIT 编译（可用 `cache=True` 缓存到磁盘）。批量实验务必**先跑一次暖机**，否则每次"第一个实验"都很慢。
6. **开启 Rust 引擎**：`vectorbt[rust]` 提供预编译内核，省掉 JIT 冷启动，适合大量短任务/CI 场景。
7. **并行策略**：Numba 内核本身多线程（`numba` 默认 parallel），此时**不要**再套 `joblib` 多进程（会争抢内存带宽）；参数分块之间用多进程更划算。

**它适合的规模**：数千只股票 × 日线 × 数百参数组合的扫描（分块执行）**完全可行**，典型耗时从"分钟级"到"小时级"。
**它不适合的规模**：分钟线全市场 × 上千参数组合 × 逐笔记录（内存会爆炸）；高频/订单簿级研究（用 NautilusTrader/自研 C++）。

### 3.6 VectorBT 结论

**适合做"参数扫描 + 快速研究 + 可视化报告"的主力引擎；不要让它承担 A股规则、资金管理细节、实盘对接。**
必须自己补的三件事：①A股交易规则适配（T+1/涨跌停/停牌/100 股/成本）；②财务数据的 PIT 对齐；③参数分块调度与结果落库（VectorBT 本身不做实验管理）。

---

## 4. Qlib 深度分析

> 分析对象：`microsoft/qlib` main 分支完整源码（7.8MB，619 个文件）
> 核心事实：49k★ / 7.7k fork / 139 贡献者 ｜ **MIT 许可（最干净）** ｜ 最新 release **v0.9.7（2025-08）** ｜ 近一年仅 **26 次提交**（2026-07-23 最后一次）

### 4.1 整体架构（源码目录实测）

```
qlib/
├── data/                # 数据层：表达式引擎 + 存储 + Dataset
│   ├── ops.py           # 表达式引擎（D.features(instruments, fields) 的算子实现）
│   ├── data.py/base.py  # Provider 抽象（LocalProvider/CalendarProvider/InstrumentProvider/FeatureProvider）
│   ├── cache.py         # 表达式缓存（MemCache/ExpressionCache/DiskExpressionCache）
│   ├── pit.py           # Point-in-Time 数据库（财务数据防未来函数）
│   ├── storage/         # file_storage.py：日历/标的/特征的本地二进制存储
│   └── dataset/         # handler.py / loader.py / processor.py / storage.py / weight.py
├── model/               # 模型抽象：base.py(模型基类) / trainer.py(训练器) / ens/ / interpret/ / risckmodel/ / meta/
├── strategy/            # 策略抽象：base.py(BaseStrategy.generate_trade_decision) / RLStrategy
├── backtest/            # 回测引擎（下面单独展开）
├── workflow/            # 实验管理：exp.py / expm.py / recorder.py / record_temp.py / __init__.py
├── contrib/             # 官方"贡献包"：数据(Alpha158/360)、模型(LGB/XGB/CatBoost/PyTorch 30+)、策略、评估、报告、滚动、调参
├── rl/                  # 强化学习框架（环境/解释器/奖励）
└── cli/                 # 命令行入口（qrun / qlib data）
```

### 4.2 逐层说明

| 层 | 关键类/文件 | 能力 |
| --- | --- | --- |
| **数据层** | `qlib/data/data.py`、`storage/file_storage.py`、`ops.py`、`pit.py` | 统一的 `D.calendar / D.instruments / D.features / D.list_instruments` 接口；自有二进制 `.bin` 存储（按 标的×字段 分片）；**表达式引擎**支持 `Ref($close,1)`、`Mean($close,5)`、`$close/Ref($close,1)-1` 这类算子；PIT 支持财务数据时点对齐 |
| **Feature/Factor** | `contrib/data/handler.py` 的 `Alpha158` / `Alpha360`、`contrib/data/loader.py` 的 `Alpha158DL/Alpha360DL`、`DataHandlerLP` | 官方内置 158/360 个技术面因子的**表达式定义**；`DataHandlerLP` 支持 `learn/infer` 双处理器链（fit 期与 infer 期可不同，天然防数据泄漏） |
| **Dataset** | `qlib/data/dataset/__init__.py:DatasetH`、`dataset/handler.py` | `segments={'train','valid','test'}` + `handler`；按时间段切分，输出 `DataFrame`（MultiIndex: datetime×instrument） |
| **Model** | `qlib/model/base.py`、`trainer.py`、`contrib/model/*` | 30+ 现成模型：`LGBModel`、`XGBoostModel`、`CatBoostModel`、`LinearModel`、`DoubleEnsemble`，以及 PyTorch 系列（LSTM/GRU/Transformer/TCN/GATs/ALSTM/TabNet/KRNN/Sandwich…）；统一 `fit()/predict()` 接口 |
| **Strategy** | `qlib/strategy/base.py`、`contrib/strategy/signal_strategy.py` | `TopkDropoutStrategy`（每期取分数前 K 名、剔除 N 名，是 A股 选股回测的经典实现）、`WeightStrategyBase`、规则策略、成本控制策略、订单生成器；`BaseStrategy.generate_trade_decision()` → `BaseTradeDecision` |
| **Backtest** | `backtest/exchange.py`、`position.py`、`account.py`、`executor.py`、`decision.py`、`report.py` | 真实 A股 规则：`check_stock_limit`(涨跌停)、`check_stock_suspended`(停牌)、`round_amount_by_trade_unit`(100 股一手)、`_clip_amount_by_volume`(成交量限制)、`open_cost/close_cost/min_cost`(佣金)；`NestedExecutor` 支持"日频决策 + 分钟级执行"两级嵌套撮合 |
| **Evaluation** | `qlib/contrib/evaluate.py` | `risk_analysis`（年化收益/波动/Sharpe/最大回撤/信息比率）、`indicator_analysis`、`long_short_backtest`、`t_run`（t 检验） |
| **Experiment/Recorder** | `workflow/recorder.py`、`exp.py`、`expm.py` | `ExperimentManager → Experiment → Recorder` 三级结构，**官方实现 `MLflowExpManager` 基于 MLflow**；可直接 `mlflow ui` 看结果 |
| **Report/可视化** | `contrib/report/*`、`graph.py` | `cumulative_return_graph`(累计收益+回撤+持仓)、`risk_analysis_graph`(月度/年度风险收益)、`score_ic_graph`(IC/ICIR/Rank IC)、`rank_label_graph`、`report_graph`、`analysis_model_performance`（模型预测分组表现） |

### 4.3 重点问题一：Qlib 是怎么保存和管理一次实验结果的？

**答案：Qlib 把"一次实验"抽象成 MLflow 的 run + 若干约定命名的 artifact 文件。** 源码证据链：

1. `qlib/workflow/expm.py` → `MLflowExpManager`（基于 `mlflow.tracking`），管理 experiment 与 recorder。
2. `qlib/workflow/recorder.py` → `Recorder` 定义统一接口：`start_run / end_run / log_params / log_metrics / log_artifact / set_tags / save_objects / load_object / list_artifacts / download_artifact`；`MLflowRecorder` 是具体实现。
3. `qlib/workflow/record_temp.py` → 用 `RecordTemp` 模板类把"实验产物"标准化：

| 记录类 | 产物 artifact | 内容 |
| --- | --- | --- |
| `SignalRecord` | `pred.pkl`、`label.pkl` | 模型对测试集的预测分数 + 真实标签 |
| `SigAnaRecord` | `sig_analysis/ic.pkl`、`ric.pkl`、`long_short_r.pkl`、`long_avg_r.pkl` | Rank IC、IC、多空收益、多头平均收益（信号层面的因子有效性） |
| `PortAnaRecord` | `portfolio_analysis/report_normal.pkl`、`positions_normal.pkl`、`port_analysis.pkl` | 组合每日收益/成本/换手/持仓明细 + 风险分析结果 |
| `MultiPassPortAnaRecord` | 同上（多次随机初始化的稳健性检验） | 打乱初始分数后重复回测，检验结果稳定性 |

4. 参数（`log_params`）、指标（`log_metrics`）、标签（`set_tags`）走 MLflow 标准通道；大文件走 artifact。
5. **存储位置可切换**：`QlibRecorder.set_uri()` 可指向本地目录（默认 `~/.qlib/qlib_data/...`）或远程 MLflow Server。

> 这就是你想要的"实验管理"能力：**每次实验 = 一份 YAML 配置 + 一个 MLflow run + 一批命名固定的 pkl artifact**，天然可追溯、可对比、可复现。

### 4.4 重点问题二：一个研究人员如何从"因子"走到"策略回测"？

Qlib 的标准路径（`qrun` 一条命令跑完，配置见 `examples/benchmarks/LightGBM/workflow_config_lightgbm_Alpha158.yaml`）：

```
① 因子定义     数据层表达式 / Alpha158 → DataHandlerLP(handler)
                         ↓
② 数据集切分   DatasetH(segments={train,valid,test})   # 时间序列切分，防泄漏
                         ↓
③ 模型训练     Model(如 LGBModel) + Trainer.fit(dataset)
                         ↓
④ 信号生成     model.predict(dataset) → SignalRecord → pred.pkl
                         ↓
⑤ 因子检验     SigAnaRecord → ic.pkl / ric.pkl / long_short_r.pkl
                         ↓
⑥ 策略构建     TopkDropoutStrategy(signal=<PRED>, topk=50, n_drop=5)
                         ↓
⑦ 组合回测     PortAnaRecord(config=port_analysis_config)
               └─ backtest: start/end_time, account, benchmark, exchange_kwargs
                  {limit_threshold: 0.095, open_cost: 0.0005, close_cost: 0.0015, min_cost: 5}
                         ↓
⑧ 结果落库     portfolio_analysis/report_normal.pkl + positions_normal.pkl + port_analysis.pkl
                         ↓
⑨ 报告可视化   contrib/report 的 *_graph 函数 / mlflow ui / Jupyter
```

**最小配置文件模板**（实测自 v0.9.7 仓库，字段已核对）：

```yaml
qlib_init:    { provider_uri: "~/.qlib/qlib_data/cn_data", region: cn }
market: csi500
benchmark: SH000905
data_handler_config:
  start_time: 2008-01-01
  end_time: 2020-08-01
  fit_start_time: 2008-01-01
  fit_end_time: 2014-12-31
  instruments: csi500
port_analysis_config:
  strategy: { class: TopkDropoutStrategy, module_path: qlib.contrib.strategy,
              kwargs: { signal: <PRED>, topk: 50, n_drop: 5 } }
  backtest: { start_time: 2017-01-01, end_time: 2020-08-01, account: 100000000,
              benchmark: SH000905,
              exchange_kwargs: { limit_threshold: 0.095, deal_price: close,
                                 open_cost: 0.0005, close_cost: 0.0015, min_cost: 5 } }
task:
  model:   { class: LGBModel, module_path: qlib.contrib.model.gbdt, kwargs: {...} }
  dataset: { class: DatasetH, module_path: qlib.data.dataset,
             kwargs: { handler: { class: Alpha158, module_path: qlib.contrib.data.handler, kwargs: {...} },
                       segments: { train: [...], valid: [...], test: [...] } } }
  record:
    - { class: SignalRecord,  module_path: qlib.workflow.record_temp, kwargs: { model: <MODEL>, dataset: <DATASET> } }
    - { class: SigAnaRecord,  module_path: qlib.workflow.record_temp, kwargs: { ana_long_short: False, ann_scaler: 252 } }
    - { class: PortAnaRecord, module_path: qlib.workflow.record_temp, kwargs: { config: *port_analysis_config } }
```

> 注意：**你要的"研究闭环"里 80% 的环节，这个 YAML 已经表达了**：因子→数据集→模型→信号→因子检验→策略→回测→落库。你的平台本质上要做的，是"用 AI 生成/修改这份配置 + 用本地数据喂给它 + 把结果变成报告"。

### 4.5 Qlib 对我们这个个人项目，是不是过于复杂？

**是，但是"值得付出学习成本的复杂"。** 具体拆解：

**过度复杂的地方（不要照搬）：**
1. **数据格式专有**：自有 `.bin` 分片 + `dump_bin` 工具，与你的 Parquet 仓库是两套东西。直接用 Qlib 数据层 = 数据被锁死在 Qlib 生态里。
2. **概念栈太深**：Provider → ExpressionCache → Handler → Processor → Dataset → Model → Strategy → Executor → Account → Position → Recorder，共 10+ 层抽象。对"只想验证一个因子想法"的人，前期成本明显高于直接 pandas。
3. **RL / Meta-Learning / Online Serving / High-Freq / Orderbook / 分布式 特性**：与你的目标（中低频个人研究）无关，属于"官方为了论文和机构场景做的"，会造成认知负担。
4. **活跃度下降**：近一年 26 次提交、最新 release 是 2025-08（v0.9.7）。意味着 bug 修复和新特性供应会变慢，遇到问题主要靠自己读源码——但它是 MIT 许可、读者可以自己改，且代码质量高，风险可控。
5. **A股 财务因子数据缺失**：Qlib 提供技术面 Alpha158/360，但财务因子（PE/PB/ROE/营收）需要你自己接数据并做 PIT 对齐。

**值得直接用的模块（强烈建议）：**

| 模块 | 用它的什么 | 怎么用 |
| --- | --- | --- |
| `qlib/backtest/exchange.py` + `position.py` + `account.py` | **A股 交易规则的现成教科书实现**（涨跌停/停牌/100 股/成交量约束/佣金印花税） | 直接调用，或按它的逻辑改写你自己的规则层 |
| `qlib/contrib/strategy/signal_strategy.py` | `TopkDropoutStrategy` 等 A股 选股组合构建 | 直接使用 |
| `qlib/workflow/*`（Recorder + MLflow） | **实验记录与产物规范** | 直接用（它能独立于 Qlib 数据层使用） |
| `qlib/contrib/report/*` | 因子/组合可视化图表函数 | 直接调用出图 |
| `qlib/contrib/evaluate.py` | 风险分析指标（年化/波动/Sharpe/回撤/IC） | 直接调用 |
| `qlib/contrib/data/handler.py` 的 Alpha158/360 | 因子表达式清单 | 抄它的因子定义（表达式是文本，容易移植到你自己的 Parquet 数据层） |
| `qlib/contrib/model/*` | LightGBM/XGBoost/CatBoost/PyTorch 模型封装 | 直接使用（脱离 Qlib 也能跑） |
| `qlib/workflow/record_temp.py` | 实验产物的命名规范 | 按它的 artifact 命名约定设计你的实验规范 |

**只值得借鉴架构、不要直接用的部分：**
`data/storage/file_storage.py`（自有 .bin 格式）、`qlib/rl/*`、`qlib/model/meta/*`（DDG-DA/元学习）、`qlib/contrib/rolling/*`（滚动训练框架，思路可借鉴）、`docs/advanced/server.rst` 的在线服务、`qlib/contrib/online/*`。

### 4.6 Qlib 的 A股 适配度（关键）

| 维度 | 结论 |
| --- | --- |
| 数据 | ✅ 官方提供 cn_data（CSI300/CSI500），`scripts/data_collector/` 下有 `baostock_5min`、`cn_index`、`crowd_source`、`yahoo`、`us_index`、`crypto`、`fund`、`pit` 采集器 → **A股 日线/5分钟线可直接落地** |
| 回测规则 | ✅ `limit_threshold: 0.095`（涨跌停）、`open_cost/close_cost/min_cost`（A股 佣金+印花税结构）、`trade_unit=100`（一手）、成交量裁剪 |
| 因子 | ✅ 技术面（Alpha158/360）；⚠️ 财务因子需自建（需接 Tushare Pro/AkShare 财务接口 + PIT 对齐，可用 `qlib/data/pit.py` 的思路） |
| 基准 | ✅ 配置里直接写 `benchmark: SH000905`（中证500），支持 CSI300 等 |
| 实盘 | ❌ 无（Qlib 定位是研究/回测；`online serving` 只是模型在线推理，不是交易通道） |

### 4.7 Qlib 结论

**把它当作"研究流水线的骨架与规则参考"，而不是"整个平台"。**
落地方式建议：**自己的 Parquet 数据仓库** + 一个自定义 Qlib `Provider`/`DataHandler`（把 Parquet 适配成 Qlib 的接口）+ 直接使用 Qlib 的 Backtest/Strategy/Recorder/Report。这样既拿到 Qlib 的成熟能力，又不被它的 `.bin` 数据格式锁死。

---

## 5. Backtrader 深度分析

> 分析对象：`mementum/backtrader` master 分支源码（6.7MB，378 文件），版本 `1.9.78.123`

### 5.1 现状（第一个必须知道的结论）

| 项 | 值 |
| --- | --- |
| Star / Fork | 23k★ / 5.3k fork（非常知名） |
| 最近提交 | **2023-04-19**（近一年 **0** 次提交，项目事实上已停更） |
| 最新版本 | 只有 PyPI `1.9.78.123`，GitHub 无 release |
| 许可 | **GPL-3.0**（强 Copyleft，个人自用无碍，商业化风险高） |
| 维护者 | 原作者 Daniel Rodriguez 已长期低活跃，社区靠 fork 续命（如 `backtrader2` 等） |

**同类替代**：如果你要的是"事件驱动 + 从回测平滑过渡到实盘"，社区已普遍转向 **NautilusTrader**（LGPL-3.0，29k★，近一年 5.4k 提交，Rust 内核）或国内的 **vnpy**。

### 5.2 架构与能力（源码实测）

```
backtrader/
├── cerebro.py        # 引擎：adddata/addstrategy/addanalyzer/addobserver/optstrategy/run/plot
├── feeds/            # 数据源：pandafeed, csvgeneric, yahoo, quandl, influxfeed, mt4csv, vchart*, ibdata, oanda
├── strategy.py       # Strategy 基类：next()/notify_order()/notify_trade()/buy()/sell()/close()
├── indicator.py + linebuffer.py + lineseries.py + lineiterator.py   # Line 元编程体系
├── broker.py + brokers/bbroker.py   # 回测撮合（Market/Limit/Stop/StopLimit、保证金、拒单）
│              brokers/ibbroker.py  # Interactive Brokers 实盘/模拟
│              brokers/oandabroker.py, vcbroker.py
├── comminfo.py       # 佣金方案（CommissionInfo、股票/期货/百分比/固定）
├── fillers.py        # 成交量填充/滑点模型（FixedSize、FixedBarPerc、BarPointPerc、Volume）
├── position.py / trade.py / order.py     # 仓位、成交、订单状态机
├── analyzers/        # 16 个分析器（见下表）
├── observers/        # broker/buysell/trades/drawdown/logreturns/timereturn/benchmark
├── sizers/           # fixedsize / percents_sizer（仓位规模）
├── plot/             # matplotlib 绘图（finance.py 蜡烛图 + 买卖点 + 指标子图 + 多资产）
└── stores/ + feeds/*  # 实盘/数据源接入层
```

**Analyzers 全清单（实测目录）**：`AnnualReturn, Calmar, DrawDown, Leverage, LogReturnsRolling, PeriodStats, Positions, PyFolio, Returns, SharpeRatio, SQN, TimeReturn, TradeAnalyzer, Transactions, VWR` — 覆盖你要求的绩效统计，其中 `PyFolio` 分析器可把结果转成 pyfolio/quantstats 的 tearsheet 输入。

| 你的关注点 | Backtrader 的实现 | 评价 |
| --- | --- | --- |
| 向量化回测 | ❌ 无 | 纯事件驱动，逐 bar 循环（Python 级），比 VectorBT 慢 2~3 个数量级 |
| 事件驱动回测 | ✅ 核心能力 | 优点：贴近真实交易时序，`next()` 里能表达复杂条件逻辑 |
| 订单模拟 | ✅ 完整 | Market/Limit/Stop/StopLimit/StopTrail/Bracket 等；订单状态机（Submitted→Accepted→Partial→Completed→Canceled/Margin/Rejected） |
| Broker | ✅ `BackBroker`（回测）+ IB/Oanda/VisualChart（实盘） | **无国内券商/CTP/QMT 适配** |
| Commission | ✅ `comminfo.py` + 各 broker 独立佣金模型 | 需自己配 A股 佣金+印花税（可按百分比+固定最小值组合） |
| Slippage | ✅ `fillers.py`（百分比/固定点位/成交量比例） | 与 VectorBT 的 `slippage` 等价，但更细（可自定义 Filler） |
| Position | ✅ `position.py` + `trade.py` | 持仓/成交/盈亏记账完整，支持加仓、部分平仓 |
| Analyzer | ✅ 16 个内置 + 可自定义 | 指标质量高，`TradeAnalyzer`/`SQN`/`VWR` 是 VectorBT 没有的 |
| Plotting | ✅ matplotlib 原生（`cerebro.plot()`） | 静态图，交互性远不如 VectorBT + Plotly；但蜡烛+买卖点+指标子图开箱可用 |

### 5.3 核心问题一：从"历史研究"过渡到"模拟盘"，Backtrader 是否比 VectorBT 更合适？

**结论：方向上"更合适"（事件驱动模型天然贴合模拟盘），但工程上"不推荐"（停更 + GPL + 无国内通道 + 性能弱）。**

理由拆解：
1. **模型匹配度**：模拟盘/实盘的本质是"每个新 bar 到来 → 决策 → 下单 → 撮合 → 更新持仓"，这是事件驱动模型。VectorBT 的向量化模型假设"信号矩阵一次性给出"，天然不适合承载"实时增量决策"。
2. **但 Backtrader 的实盘接入是死路**：`brokers/` 只有 IB / Oanda / VisualChart 三家，**没有 CTP / QMT / Ptrade / 华泰 / 同花顺**等国内通道。你要写自己的 Store/Broker，而项目已停更，等于独自维护一个废弃框架的分支。
3. **真正正确的路径**：研究阶段用 **VectorBT/Qlib**（离线批量），进入模拟盘/小资金实盘阶段，用 **vnpy**（国内 CTP/QMT/股票接口生态最全，45k★，MIT）或**券商官方的 QMT/Ptrade**（合规、稳定、有模拟盘）。两者之间用**信号文件/数据库**解耦，而不是在同一框架里既要又要。
4. **如果你确实想要"开源事件驱动 + 回测到实盘一套代码"**：选 **NautilusTrader**（Rust 内核，同一套代码跑回测/沙盒/实盘，5.4k 提交/年），代价是 A股 适配要自己做、学习曲线陡。

### 5.4 核心问题二：VectorBT + Backtrader 组合使用有价值吗？

**明确回答：没有价值，不建议组合。** 理由：

1. **数据模型不兼容**：VectorBT 是"宽表 + 广播 + 列=参数/标的"；Backtrader 是"逐 bar 事件流 + Line 对象"。把 VectorBT 的扫描结果喂给 Backtrader 需要重造信号对齐、成本模型、下单时点，两侧结果几乎不可能严格一致——**回测结果不可比，这是量化研究的大忌**。
2. **存在重复能力**：手续费、滑点、仓位、止损止盈、绩效分析，两者都有；同时维护两套成本假设 = 双倍出错概率。
3. **维护成本 > 收益**：你需要的是"一次写好的信号，多处执行"，正确的做法是**把信号/权重标准化成 Parquet/DataFrame 的中间产物**，需要时再接入任何执行引擎（Qlib 的 Strategy、vnpy 的 CtaStrategy、券商的 QMT 脚本）。

**唯一合理的"组合"形态**（推荐）：
```
VectorBT / Qlib   →   （标准化信号/权重表：date × symbol × weight）   →   vnpy / QMT 模拟盘
   研究/扫描                    单一事实来源（Parquet）                     执行/下单
```
注意这不是"两个框架互相调用"，而是"共享同一份中间产物"。

---

## 6. quant-lab 深度分析

### 6.1 先解决"quant-lab 到底指哪个仓库"的问题

GitHub 检索（`quant-lab` / `quantlab` in:name，按 star 排序）实测结果，**没有任何一个项目达到 Qlib/VectorBT 量级**：

| 仓库 | Star | License | 实际定位 |
| --- | --- | --- | --- |
| `hummingbot/quants-lab` | 297 | Apache-2.0 | Hummingbot 生态的**加密货币**量化研究框架（v2.0.0，Python≥3.12） |
| `quant-dhawan/quant-dhawan-lab` | 105 | MIT | Instagram 数学动画的教学代码 |
| `sqyangx/quantlab` | 74 | 无 | A股"vibe trading"实验项目（中文，自然语言驱动） |
| `husainm97/quant-lab-alpha` | 36 | MIT | 投资分析平台（欧美数据） |
| `BaptisteZloch/Quant-Invest-Lab` | 30 | MIT | 基础量化工具集 |
| `GDUF-QUANTLAB/alpha-lab` | 13 | MIT | 高性能 alpha 挖掘框架（ClickHouse + 因子计算） |

**结论：本报告按最可能被指代的 `hummingbot/quants-lab` 做深入分析**（它是唯一"有一定使用量 + 有完整架构 + 活跃维护"的同名项目）。若你指的是其他仓库，请告我仓库全名，我重做第 6 节。

### 6.2 hummingbot/quants-lab 深度分析（源码实测）

**定位**：Python 框架，为 Hummingbot 交易策略提供"数据采集 → 特征 → 回测 → 部署"的量化研究流水线。**数据源与交易对象 100% 是加密货币**（OKX DEX、Meteora 池、资金费率、未平仓量 OI 等）。

**项目架构（实测目录）：**

```
quants-lab/
├── cli.py                     # 命令行入口
├── config/*.yml               # 配置驱动：tf_pipeline.yml / template_1_candles_optimization.yml ...
├── core/
│   ├── backtesting/           # engine.py（回测）/ optimizer.py（参数优化）/ triple_barrier_method.py（三重障碍标注）
│   ├── data_sources/          # clob.py / gateway.py / hummingbot_database.py
│   ├── data_structures/       # candles.py / backtesting_result.py / controller_performance.py / trading_rules.py
│   ├── features/              # feature_base.py / models.py / storage.py（特征工程与持久化）
│   ├── services/              # client_base.py / mongodb_client.py / okx_dex_api.py
│   ├── notifiers/             # 通知（Telegram 等）
│   └── tasks/                 # 通用任务抽象
├── app/
│   ├── controllers/           # 策略控制器（generate/market …）
│   └── tasks/                 # 具体任务：candles_downloader / funding_rates / pools_screener /
│                              #   simple_candles_downloader / trades_downloader / stat_arb_deployment /
│                              #   trend_example_backtesting / macd_bb_backtesting / notebook_task / deployment_base
├── research_notebooks/        # 研究用 Jupyter：download_candles / feature_engineering / modelling /
│                              #   market_analysis / account_report / bot_performance / load_candles_from_cache
├── scripts/ + Makefile + Dockerfile
└── docker-compose-db.yml      # MongoDB（+ mongo-express）容器
```

| 你要考察的维度 | quants-lab 的实现 |
| --- | --- |
| 项目架构 | **配置驱动（YAML）+ 任务插件化**：`cli.py` + `config/*.yml` + `app/tasks/*_task.py`，任务可"本地运行"或"Docker 部署为常驻任务" |
| 数据层 | MongoDB 作时序存储（`mongodb_client.py`），数据源是交易所 API（OKX DEX、Hummingbot Gateway）、链上池子数据；有本地 cache 更新任务 |
| 策略层 | 用 Hummingbot 的 controller/strategy 概念，策略 = 控制器（generate/market 等） |
| 回测 | `core/backtesting/engine.py` + `optimizer.py`（参数寻优）+ `triple_barrier_method.py`（**三重障碍标注**，这一块设计值得借鉴） |
| Research | `research_notebooks/` 与生产任务**分离**（下载/特征/建模/分析各有 notebook），是很好的工程习惯 |
| 模拟交易 | 通过 Hummingbot 部署任务 + 账户/机器人表现报告 notebook（`account_report.ipynb`/`bot_performance.ipynb`） |
| 数据库 | MongoDB（文档型），适合半结构化行情/池子数据；**不适合严谨的 A股 面板数据与因子研究** |
| Web 展示 | **无独立 Web 前端**（用 notebook + 报告 + 通知） |
| 报告 | Jupyter notebook 报告 + notifiers（Telegram 等） |

### 6.3 哪些代码/设计思想可以直接复用？

| 可复用项 | 怎么用 |
| --- | --- |
| **配置驱动 + 任务插件化**（`config/*.yml` + `app/tasks/*_task.py`） | ✅ 强烈建议借鉴：你的平台用 `config/experiments/*.yaml` 描述实验，任务统一注册与执行 |
| **research_notebooks 与生产任务分离** | ✅ 借鉴：研究态（Jupyter/脚本）与稳定态（定时任务/服务）分层，避免研究代码污染生产 |
| `triple_barrier_method.py`（三重障碍标注） | ✅ 可直接借鉴实现（López de Prado 的 Triple Barrier，用于给 ML 模型生成标签）；**VectorBT 开源版没有这个，PyBroker 也没有** |
| `backtesting_result.py` / `controller_performance.py` 数据结构 | ✅ 借鉴"回测结果标准化对象"的设计 |
| `notifiers/`（Telegram 通知） | ✅ 可直接用（VectorBT 开源版也有 `messaging/telegram.py`） |
| 数据下载任务 + 本地 cache 更新 | ✅ 借鉴任务模式；但**不要用 MongoDB** 存 A股 面板数据 |
| Docker Compose 起数据库 | ✅ 借鉴（但你用 DuckDB 就不需要容器） |

### 6.4 它与"我们要建的 A股 个人量化实验室"的异同

| 维度 | quants-lab | 我们的实验室 |
| --- | --- | --- |
| 市场 | 加密货币（7×24、无涨跌停、T+0、无复权） | **A股**（T+1、涨跌停、停牌、100 股、复权、财务 PIT） |
| 数据 | 交易所 API 实时拉取 + MongoDB | **本地数据仓库（Parquet/DuckDB）优先，不重复联网** |
| 策略类型 | 加密套利/趋势/做市，逐笔/分钟级 | 中低频、因子选股、日线为主 |
| 研究入口 | Jupyter notebook | Jupyter notebook + **AI Agent 生成实验** |
| 核心诉求 | 快速迭代 + 部署交易机器人 | **长期可复现的研究闭环 + 实验管理 + 因子资产积累** |
| 复杂度 | 中（Docker/Mongo/任务调度） | 起步应更简单（单机 Python + Parquet） |

**总结论：quants-lab 不适合整体采用（市场不匹配），但它的"配置驱动任务 + 研究/生产分离 + 三重障碍标注 + 结果标准化对象"四点设计值得直接抄。** 如果你的 `quant-lab` 指的是别的项目，结论可能完全不同。

---

## 7. 其他值得采用的开源项目

> 筛选标准：①有明确不可替代的能力；②许可可接受；③仍在维护或足够稳定；④与"个人 A股 中低频研究"相关。每项都给出"是否采用"的明确结论。

### 7.1 回测框架（除 VectorBT/Qlib/Backtrader 之外）

| 项目 | 为什么值得加入比较 | 许可/活跃度 | 采用建议 |
| --- | --- | --- | --- |
| **kernc/backtesting.py** | 单文件级核心、API 极简、**内置参数网格搜索 + 结果可视化**（`optimize()` 直接返回参数热力图），是"快速验证想法"的最快路径 | **AGPL-3.0**｜9k★｜33/年 | ❌ 不采用。AGPL 对任何"对外提供服务"都触发开源义务，且无多标的组合能力。**仅作思路参考** |
| **nautechsystems/nautilus_trader** | 目前最强的开源"回测→沙盒→实盘同构"引擎：Rust 内核 + Python API，事件驱动，撮合精度高，性能极强 | LGPL-3.0｜29k★｜**5.4k/年（最活跃）** | ⚠️ 现在不引入。**A股 无适配**、学习曲线陡；当你要做分钟级/多市场并需要"回测代码直接上实盘"时再评估 |
| **QuantConnect/Lean** | 机构级多资产引擎，C# + Python 双语言，本地/云端一致 | Apache-2.0｜22k★｜427/年 | ❌ 不采用。A股 数据与通道弱，运行环境重（.NET），与个人单机研究定位不符 |
| **edtechre/pybroker** | ML 友好：**walk-forward 分析、bootstrap 置信区间指标、多时间周期、动态标的池**；**自带 AI Agent 集成**（仓库有 `skills/` 目录：SKILL.md + openai.yaml + .pyi 类型桩 + CLAUDE.md，可直接给 Codex/Claude 使用） | **Apache-2.0 + Commons Clause**｜3.5k★｜448/年 | ⚠️ 选择性借鉴。**它的 `skills/` 目录是本次调研中"AI Agent 集成量化框架"的最佳范式，强烈建议照抄这套组织方式**；引擎本身不必引入 |
| **pmorissette/bt** | 极简的组合权重/再平衡回测（`bt.Backtest(strategy, data, weights)`），做资产配置/ETF 轮动非常顺手 | MIT｜3k★｜171/年 | ◐ 可选。如果你的研究包含"ETF 轮动/资产配置"，它比 VectorBT 更直观；否则不需要 |
| **stefan-jansen/zipline-reloaded** | Quantopian 血统的事件驱动引擎，`Pipeline` API 的横截面因子计算设计优雅 | Apache-2.0｜1.9k★｜**4/年（2025-11 后无提交）** | ❌ 不采用。活跃度过低 + A股 无适配 + 生态萎缩 |

### 7.2 因子研究 / 绩效分析

| 项目 | 为什么值得加入比较 | 许可/活跃度 | 采用建议 |
| --- | --- | --- | --- |
| **stefan-jansen/alphalens-reloaded** | **因子有效性检验的事实标准**：IC/Rank IC、IC 衰减、分位数收益、因子换手率、行业中性化后的分层收益、全套 tearsheet 图表 | Apache-2.0｜646★｜0/年（稳定） | ✅ **采用**。因子验证环节直接用它，不要自己写 IC/分层收益 |
| **ranaroussi/quantstats** | 60+ 绩效指标（Sharpe/Sortino/Calmar/Omega/Tail Ratio/最大回撤/连胜…）+ 一键 HTML tearsheet（含月度热力图、滚动指标、回撤区间、Monte Carlo） | Apache-2.0｜7.6k★｜13/年 | ✅ **采用**。VectorBT 已内置 `qs` 适配器（`pf.qs`），零成本集成 |
| stefan-jansen/pyfolio-reloaded | 老牌组合绩效分析（风险分解、因子暴露、持仓集中度） | Apache-2.0｜613★｜0/年 | ⚠️ 已被 QuantStats 大幅覆盖，非必需 |

### 7.3 数据处理与存储

| 项目 | 为什么值得采用 | 许可/活跃度 | 采用建议 |
| --- | --- | --- | --- |
| **duckdb/duckdb** | 单文件嵌入式分析数据库，直接 `SELECT ... FROM 'xxx.parquet'`，支持窗口函数、as-of join，秒级查询千万行 | MIT｜41k★｜23k/年 | ✅ **采用**（数据查询层） |
| **pola-rs/polars** | Rust 实现的列式 DataFrame，惰性求值（LazyFrame）、多核并行、内存效率远优于 pandas；API 与 pandas 相似 | MIT｜40k★｜2.3k/年 | ✅ **采用**（数据处理主力，pandas 仅做生态兼容） |
| **apache/arrow (PyArrow)** | 列式内存格式 + Parquet 读写实现，是 Polars/DuckDB/pandas 的共同底座 | Apache-2.0｜17k★｜1.3k/年 | ✅ **间接使用**（Parquet 读写） |
| **iterative/dvc** | 数据/模型的 Git 式版本管理（`dvc add data/`），能把数据版本与实验结果绑定 | Apache-2.0｜16k★｜84/年 | ◐ 可选。数据规模不大时用"日期分区 + 校验和清单"就够了 |

### 7.4 实验管理

| 项目 | 为什么值得采用 | 许可/活跃度 | 采用建议 |
| --- | --- | --- | --- |
| **mlflow/mlflow** | 实验追踪（params/metrics/tags/artifacts）事实标准，`mlflow ui` 一行命令开可视化面板；**Qlib Recorder 的底层实现就是它** | Apache-2.0｜28k★｜4.7k/年 | ✅ **采用**（唯一实验管理入口） |
| Qlib Recorder | 在 MLflow 之上封装了量化语义（pred/label/IC/组合报告 的固定 artifact 命名） | MIT | ✅ **采用其命名规范**，底层仍是 MLflow |

### 7.5 可视化与展示

| 项目 | 为什么值得采用 | 许可/活跃度 | 采用建议 |
| --- | --- | --- | --- |
| **plotly/plotly.py** | 交互式图表事实标准（缩放/悬浮/联动），`kaleido` 可导出静态图用于报告 | MIT｜19k★｜671/年 | ✅ **采用**（所有自建图表的底层） |
| **streamlit/streamlit** | 用纯 Python 快速搭研究看板/参数查询页，无需前端 | Apache-2.0｜46k★｜2.3k/年 | ✅ **采用**（第一版 Web 层） |
| plotly/dash | 更灵活的分析型仪表盘（回调机制），适合复杂交互 | MIT｜24k★｜1.3k/年 | ◐ Streamlit 不够用时再换 |
| gradio-app/gradio | 快速把 Python 函数变成 UI，适合做"AI 研究助手"的对话式入口 | Apache-2.0｜44k★｜780/年 | ◐ 可选（做 Agent 界面时） |

### 7.6 A股 生态（这是本次调研最需要补充的部分）

| 项目 | 为什么值得加入比较 | 许可/活跃度 | 采用建议 |
| --- | --- | --- | --- |
| **fasiondog/hikyuu** | **唯一"深度适配 A股 + 超高速 C++ 内核 + 完整策略组件化"的活跃开源项目**：模块化组件覆盖 市场环境研判/信号指标/盈亏风控/资金配比/收益目标/滑点模拟/多因子建模/组合分析；自带 TDX(pytdx) 数据接入、Bokeh/pyecharts 绘图、PySide6 GUI；README 明确"A股 + QMT 合规扩展" | Apache-2.0｜3.5k★｜**1.3k/年（很活跃）** | ✅ **强烈建议纳入技术选型**。适合作为"VectorBT/Qlib 之外的 A股 原生备选引擎与数据接入参考"；中文文档、国内社区 |
| **yutiansut/QUANTAXIS** | A股 全流程最完整的国产项目：`QAData`（数据）`QAFactor`（因子）`QAEngine`（回测引擎）`QAStrategy`（策略）`QAWebServer`（Web）`QAPubSub`（消息）`QARSBridge`（Rust 桥），架构野心大 | MIT｜11k★｜29/年（最近提交 2026-09-01，但 Web 端 2020 年后停滞） | ◐ **借鉴架构，不建议整体采用**。可复用其"数据→因子→策略→Web"分层思想与 QAFactor 的因子组织方式；但组件过多、维护不均，个人项目引入成本高 |
| **zvtvz/zvt** | A股 数据+因子+ML+交易一体化，`factors/`（含 technical_factor, factor_models, shape, target_selector, transformers）设计清晰 | MIT｜4.3k★｜12/年 | ◐ 借鉴其因子层与 `ml/lables.py` 标签设计；活跃度低，不建议作为主干 |
| **vnpy/vnpy** | 国内实盘交易第一生态：CTP（期货）、股票（QMT/华鑫奇点等）、统一 EventEngine + 策略模板 + 图形界面 | MIT｜45k★｜54/年 | ✅ **模拟盘/实盘阶段采用**（不是研究框架） |
| **akfamily/akshare** | 免费 A股/财经数据接口库，覆盖行情、财务、指数、行业、宏观、另类数据 | MIT｜23k★｜374/年 | ✅ **采用（数据采集主力）** |
| waditu/tushare | A股 数据 SDK；**开源仓库 2020 年后停更**，实际数据服务为付费的 Tushare Pro（积分制） | BSD-3-Clause｜15k★｜0/年 | ◐ 作为**付费数据源**（质量高于免费源，尤其是财务数据）；不要依赖其旧开源仓库 |
| baostock / pytdx | 免费数据源：baostock（历史行情+财务，需登录）、pytdx（通达信行情协议，hikyuu 已集成） | 宽松 | ✅ 作为免费补充源（注意稳定性与合规） |

### 7.7 AI + Quant（你的差异化核心）

| 项目 | 为什么值得加入比较 | 许可 | 采用建议 |
| --- | --- | --- | --- |
| **microsoft/RD-Agent** | 微软官方 LLM 自动研发 Agent，**与 Qlib 深度集成**，能自动做"因子挖掘"与"模型优化"闭环（Qlib README 首页即为它做推荐）；论文 R&D-Agent-Quant (arXiv:2505.15155) | MIT（需另行确认） | ✅ **作为 AI Agent 架构的首要参考**，第一期先抄"研究假设→代码生成→回测→评估→迭代"的 loop 设计，不直接托管资金 |
| **edtechre/pybroker 的 `skills/` 目录** | 把"框架能力"打包成 AI Agent 可直接消费的形态：每个 skill 一个 `SKILL.md` + `openai.yaml` + `.pyi` 类型桩 + 模板代码 + wiki 片段，并用 `CLAUDE.md` 约束 | Apache-2.0 + Commons Clause | ✅ **直接照抄目录组织方式**（这是本次调研中"量化框架 + AI Agent"结合得最好的工程实践） |
| **jlowin/fastmcp** | 用 Python 快速把函数暴露为 MCP 工具（`@mcp.tool()`），让 Codex/Claude 等直接调用你的平台 | Apache-2.0｜28k★｜1.8k/年 | ✅ **采用**（AI 与平台之间的标准接口层） |

### 7.8 明确不推荐清单（附原因）

| 项目 | 不推荐原因 |
| --- | --- |
| Backtrader | 2023-04 起停更、GPL-3.0、无国内实盘通道、性能落后 |
| backtesting.py | AGPL-3.0 传染性最强（连 SaaS 都触发开源） |
| 原版 Zipline | 已被 Quantopian 归档，社区已迁移到 zipline-reloaded（而后者也低活跃） |
| QUANTAXIS 的 Web/服务端 | 2020 年后基本停滞，不要作为新平台底座 |
| Tushare 开源仓库 | 2020 年起停更（数据服务另行付费） |
| 自研回测引擎 / 自研图表库 / 自研实验管理系统 | 三个领域都有成熟方案，自研是纯粹浪费 |

---

## 8. 哪些轮子直接复用

> 原则：**能复用就不要自己写**。下表逐模块给出结论，并标注"自己写的部分要写多少"。

| 模块 | 是否已有成熟开源 | 推荐项目 | 是否直接使用 | 需要自己开发的部分 |
| --- | --- | --- | --- | --- |
| **数据采集** | ✅ 成熟 | AkShare（免费主力）、Tushare Pro（付费财务）、Baostock、pytdx；hikyuu 内置 pytdx 接入 | ✅ 直接用 | 只写**调度与落地**：增量更新、断点续传、复权因子同步、数据质量校验（缺失/重复/异常价格） |
| **数据仓库** | ✅ 成熟 | Parquet（格式）+ DuckDB（查询）+ SQLite（元数据） | ✅ 直接用 | 只写**目录规范与写入器**（分区键、命名、schema 校验）；不要写数据库内核 |
| **Feature（特征计算）** | ✅ 成熟 | Polars（LazyFrame）、pandas `.rolling/.ewm`、VectorBT 的 `.vbt.rolling_*` 访问器 | ✅ 直接用 | 写特征注册表（名称→表达式→参数→依赖），避免散落脚本 |
| **Factor（因子库 + 因子检验）** | ✅ 成熟 | **alphalens-reloaded**（IC/分层/换手/衰减）+ Qlib Alpha158 表达式清单 | ✅ 直接用 | 写**因子定义与版本管理 + 横截面/行业中性化流程**；不要自己写 IC/分层算法 |
| **回测** | ✅ 成熟 | **Qlib**（组合/ML/多因子，含 A股 规则）+ **VectorBT**（信号型/参数扫描） | ✅ 直接用 | 写**A股 规则适配层**（T+1/涨跌停/停牌/ST/最小 100 股）与两引擎的成本一致性校验 |
| **参数扫描** | ✅ 成熟 | **VectorBT**（列=参数，一次算完） | ✅ 直接用 | 写**分块调度器**（按参数/股票分批、内存上限控制、结果落库） |
| **统计分析** | ✅ 成熟 | QuantStats（绩效）、alphalens-reloaded（因子）、scipy/statsmodels（显著性/t 检验）、Qlib `contrib/evaluate.py` | ✅ 直接用 | 写"多重检验校正"流程（防 p-hacking）：样本外 + 参数稳定性 + Bonferroni/FDR |
| **可视化** | ✅ 成熟 | Plotly（图表）+ QuantStats/Qlib report（图组）+ Streamlit（Web）+ VectorBT（回测图） | ✅ 直接用 | 只写**报告组装器**（把指标+图拼成可归档的 HTML/PDF，并写入实验记录） |
| **实验管理** | ✅ 成熟 | **MLflow**（Qlib Recorder 底层）+ Qlib 的 artifact 命名规范 | ✅ 直接用 | 写**实验元数据规范**（一次实验必须记录：数据版本/因子版本/参数/随机种子/代码 commit） |
| **模拟盘** | ◐ 部分成熟 | vnpy（国内通道全）或券商 QMT/Ptrade 官方模拟 | ◐ 半自建 | 写**信号发布与执行解耦层**（研究侧产出权重表 → 执行侧读取 → 下单 → 回报落库）；不要在研究框架里直连交易 |
| **实盘** | ◐ 部分成熟 | vnpy + QMT/Ptrade（合规终端） | ◐ 半自建 | 写**风控开关**（单笔上限/单日亏损熔断/黑名单/人工确认）、持仓对账、异常告警 |
| **策略管理** | ◐ 无现成合适的 | 借鉴 quants-lab 的配置驱动任务 + Qlib 的 YAML 工作流 | ◐ 自建（薄） | 写**策略注册表与生命周期**（研究→候选→模拟→小资金→运行→暂停→淘汰），这是你的核心资产之一 |
| **AI 研究助手** | ◐ 部分成熟 | **FastMCP**（工具层）+ pybroker `skills/` 组织方式 + **RD-Agent**（loop 设计） | ◐ 自建（薄） | 写**工具契约与安全边界**（哪些操作允许 LLM 触发、哪些必须人工确认）；这是你的差异化资产 |

**一句话**：13 个模块里 **9 个可以完全复用**，3 个"半自建"（模拟盘/实盘/策略管理），1 个"薄自建"（AI 助手）。**没有任何一个模块需要你从零造轮子。**

---

## 9. 哪些东西必须自己开发

下面 5 项是"别人给不了、且决定你研究效率"的部分——**这也是本报告唯一建议你写代码的地方**。

### 9.1 必须自研清单

| # | 自研模块 | 为什么必须自己写 | 预估工作量 |
| --- | --- | --- | --- |
| 1 | **A股数据仓库与数据管道** | 数据源接口、复权、停牌、财务 PIT、行业分类、退市股票（生存者偏差）处理规则**没有通用实现**；且这是你研究结论正确性的根基 | 大（P0 阶段 2~3 周起步，长期迭代） |
| 2 | **A股交易规则适配层** | T+1、涨跌停不能成交、停牌、最小 100 股、ST/退市、印花税/佣金/过户费——Qlib 只覆盖一部分且封装在内部；VectorBT 完全没有 | 中（1~2 周，逻辑清晰可测试） |
| 3 | **因子库 + 因子注册表** | 因子是研究资产；需要"定义→计算→缓存→版本→检验→复用"的闭环 | 中（与因子数量线性相关） |
| 4 | **实验元数据规范 + 策略生命周期管理** | 让"第 37 次实验"和"第 3 次实验"可比较、可复现、可淘汰；框架不提供你的业务语义 | 小~中（1 周，但必须有） |
| 5 | **AI 研究助手工具层（MCP）+ 安全边界** | 把上面 4 项封装成 LLM 可调用的稳定接口，并限制它"只能研究、不能下单" | 中（MCP server + 工具契约 1~2 周） |

### 9.2 明确不要自研清单（重复造轮子警告）

| 不要写 | 用现成的 |
| --- | --- |
| 回测撮合引擎（订单/持仓/盈亏计算） | Qlib Backtest / VectorBT Portfolio |
| 参数扫描框架 | VectorBT 的列广播 |
| 绩效指标（Sharpe/回撤/Sortino/Calmar…） | QuantStats / VectorBT returns accessor / Qlib evaluate |
| IC/分层收益/因子换手 | alphalens-reloaded |
| 净值/回撤/买卖点/热力图绘制 | VectorBT plotting / Plotly |
| 实验参数记录与对比界面 | MLflow UI |
| 数据框 join/groupby/rolling 的加速实现 | Polars / DuckDB |
| 复权算法本身（前复权/后复权公式） | AkShare/pytdx 提供的复权因子 + Polars 实现（公式简单，但**必须自己校验**） |

---

## 10. 数据架构建议

### 10.1 先算清楚数据规模（决定选型的关键）

| 数据 | 估算 | 存储（Parquet + zstd） |
| --- | --- | --- |
| 3000 只 A股 × 10 年日线 × 12 字段 | 约 730 万行 | **约 150~300 MB** |
| 同上 + 复权因子 + 停牌/涨跌停标记 | 约 730 万行 × 18 字段 | 约 300~500 MB |
| 财务数据（季度，3000 只 × 10 年 × 200 指标） | 约 120 万行 | 约 100 MB |
| 因子面板（50 个因子 × 3000 只 × 10 年） | **1.8 亿单元格** | 约 700 MB ~ 1.5 GB |
| 分钟线（若将来做） | 3000 只 × 10 年 × 240 bar | **百 GB 级**（需另设计） |

> **结论：你未来 3~5 年的日线研究数据量在"几 GB"级别。** 这个量级下 PostgreSQL/ClickHouse/Spark 是纯粹的过度工程。

### 10.2 技术对比与推荐

| 候选 | 定位 | 分析查询 | 并发写 | 运维成本 | 在本项目中的角色 |
| --- | --- | --- | --- | --- | --- |
| **Parquet** | 列式文件格式 | ⭐⭐⭐⭐（配合 DuckDB/Polars） | ❌（不可变文件） | 极低 | ✅ **唯一的事实存储**（行情/财务/因子） |
| **DuckDB** | 嵌入式 OLAP 数据库 | ⭐⭐⭐⭐⭐（向量化、窗口函数、**ASOF JOIN**） | ⚠️ 单写多读 | 极低（一个文件/进程内） | ✅ **查询层**（直接查 Parquet，无需导入） |
| **SQLite** | 嵌入式 OLTP 数据库 | ⭐⭐ | ✅ 单写多读 | 极低 | ✅ **元数据/状态**（实验登记、任务状态、策略清单、配置快照） |
| **Polars** | 列式 DataFrame 引擎 | ⭐⭐⭐⭐⭐（LazyFrame、多核） | — | 极低 | ✅ **计算层**（清洗/因子计算）；pandas 仅用于生态兼容 |
| **PostgreSQL** | 服务器型关系库 | ⭐⭐⭐⭐ | ✅ 高并发 | 中高（服务、备份、调优） | ❌ **现在不要引入**（个人单机无并发写需求） |
| **ClickHouse** | 分布式列存 | ⭐⭐⭐⭐⭐ | ✅ | 高 | ❌ **现在不要引入**（几 GB 数据用不上） |
| **MongoDB** | 文档数据库 | ⭐⭐ | ✅ | 中（容器） | ❌ 不适合面板数据的横截面分析（quants-lab 用它是因为加密池子数据半结构化） |
| **Qlib `.bin` 格式** | Qlib 专有 | ⭐⭐⭐⭐⭐（Qlib 内部） | ❌ | 低 | ◐ 只在使用 Qlib 时作为**派生视图**，不要作为主存储 |

**最终推荐（三层）：**

```
① 事实存储：Parquet（分区 + zstd 压缩）        ← 唯一可信数据源
② 查询引擎：DuckDB（直接 SELECT FROM parquet）  ← 零导入、支持 ASOF JOIN
③ 元数据：SQLite（实验/任务/策略/配置快照）      ← 小、事务安全
  计算层：Polars（主力）/ pandas（兼容）
```

### 10.3 建议的目录结构

```
quantlab/
├── data/
│   ├── raw/                     # 原始落地（不可变，保留 source 原始字段）
│   │   └── source=akshare/freq=daily/year=2024/part-0.parquet
│   ├── market/daily/            # 清洗后行情（复权因子、停牌、涨跌停标记）
│   │   └── year=2024/data.parquet
│   ├── financial/               # 财务（含 ann_date 用于 PIT）
│   │   └── year=2024/income.parquet
│   ├── index/                   # 指数、行业分类、板块
│   ├── factor/                  # 计算后的因子面板（按因子名分区）
│   │   └── factor=momentum_20/year=2024/data.parquet
│   └── meta/                    # 交易日历、股票列表（含退市）、schema 版本
├── experiments/                 # 每次实验一份目录（与 MLflow run 对应）
│   └── 2026-09-14_momentum_20_a1b2c3/
│       ├── config.yaml          # 完整配置（数据版本/因子/参数/成本假设）
│       ├── metrics.json         # 关键指标
│       ├── report.html          # 可视化报告
│       └── artifacts/           # pred.pkl / positions.parquet / trades.parquet
├── src/quantlab/                # 你自己的代码（数据管道/规则层/因子/Agent 工具）
├── notebooks/                   # 研究态脚本（与生产代码分离）
└── configs/                     # 数据源配置、成本假设、策略模板
```

### 10.4 关键数据处理规则（容易被忽略，但决定研究正确性）

| 问题 | 处理方式 |
| --- | --- |
| **复权** | 保存原始 OHLCV + `adj_factor` 两列，**查询时计算复权价**（不要存多套复权价）；回测用前复权做信号、用原始价+复权因子算成交金额，避免"未来复权因子"泄漏 |
| **财务数据 PIT** | 必须同时存 `end_date`（报告期）与 `ann_date`（公告日）；查询用 **DuckDB `ASOF JOIN`** 取 `ann_date <= 交易日` 的最新一期，**否则就是未来函数** |
| **停牌** | 存 `is_suspended` 标记；停牌日不产生成交（Qlib `check_stock_suspended` 逻辑可借鉴） |
| **涨跌停** | 存 `limit_up/limit_down` 价格；开盘即涨停且无量时模拟"买入失败"（Qlib `check_stock_limit`/`_clip_amount_by_volume` 可借鉴） |
| **退市/生存者偏差** | 股票列表必须包含**已退市股票**及其退市日期，否则回测收益被系统性高估 |
| **交易日历** | 统一用上交所交易日历作为唯一基准；所有数据必须与日历对齐（缺失 → 明确标记，不要静默 ffill） |
| **数据质量校验** | 每日入库跑断言：`low <= open,close <= high`、`volume >= 0`、日期唯一、涨跌幅在合理区间（|涨跌幅| ≤ 21% 或标记异常） |

### 10.5 与 Qlib 的衔接方式（推荐）

```
Parquet 数据仓库（你的主权数据）
        │
        ├─ 方案一（推荐）：写一个自定义 Qlib Provider/DataHandler，把 Parquet 适配成 Qlib 的 D.features() 接口
        │      优点：数据主权在自己手里；缺点：需要读 qlib/data/base.py 的 Provider 抽象
        │
        └─ 方案二（备选）：定期用 qlib 的 dump_bin 把 Parquet 导出成 Qlib .bin
               优点：零适配成本；缺点：数据双份、更新延迟、格式锁死
```
建议**先用方案二跑通闭环（1~2 天）**，再逐步替换为方案一。

---

## 11. 回测架构建议

### 11.1 四层解耦（关键设计）

```
① 信号层（向量化，快）        entries/exits 或 因子分值
        ↓  输出统一中间格式：Parquet/DataFrame（date × symbol × score/weight）
② 组合层（权重/持仓）         TopK 选股、等权/波动率倒数/风险平价、换手约束、行业中性
        ↓  输出：目标权重表（date × symbol）
③ 撮合层（A股规则，慢但必须准）  T+1、涨跌停不可成交、停牌、最小 100 股、佣金/印花税/过户费
        ↓  输出：成交明细 + 每日持仓 + 每日净值（统一 schema）
④ 评估层（统计与报告）         QuantStats 指标 + 回撤/归因 + 因子 IC + HTML 报告
```

**为什么必须解耦**：扫参数用 ①+② 的快速路径（VectorBT），最终验证用 ③ 的精确路径（Qlib 或自研规则层）。**如果两处用不同的成本/规则假设，你的扫描结果就是幻觉。**

### 11.2 双引擎分工（明确）

| 场景 | 引擎 | 说明 |
| --- | --- | --- |
| 单因子/多因子快速扫描、参数网格、参数稳定性热力图 | **VectorBT** | 一次算数百~数千组合，分钟级出结果 |
| 多因子合成、ML 模型（LightGBM/线性）、组合优化、完整风控回测 | **Qlib** | `DatasetH` + `TopkDropoutStrategy` + `Exchange`（含 A股 规则） |
| 事件驱动逐 bar 逻辑（复杂条件、动态调仓、盘中执行） | **不引入**（如确需：NautilusTrader） | Backtrader 已停更，不推荐 |
| 模拟盘/实盘 | **vnpy / QMT** | 与研究框架解耦，只消费"目标权重表" |

### 11.3 一致性校验（必须有，否则前面全白做）

1. **交叉验证**：随机抽 20 个策略，同时用 VectorBT 与 Qlib 跑，净值曲线差异应 < 1%（差异来源必须能解释：成本模型/成交价格/停牌处理）。
2. **规则单元测试**：为 T+1、涨跌停、停牌、100 股、费用各写一个最小可验证用例（如"涨停日买入应为 0 成交"）。
3. **极限测试**：全仓单只股票、零成本、买入持有 → 结果应与基准指数/买入持有收益一致。
4. **参数敏感度**：把佣金率 ±50%，观察策略收益衰减——**对成本极度敏感的策略在高频化时必然死亡**。

### 11.4 防过拟合的强制流程（写进平台，不靠自律）

| 机制 | 实现 |
| --- | --- |
| 样本内/样本外切分 | `DatasetH(segments={train, valid, test})`，test 只在最后跑一次 |
| Walk-forward | VectorBT `RollingSplitter/ExpandingSplitter`；Qlib `contrib/rolling` 的思路 |
| 参数稳定性 | 参数热力图看"稳定高原"而非"孤立峰值"（VectorBT `.vbt.heatmap`） |
| 多重检验校正 | 记录实验总次数，对 p 值做 Bonferroni/FDR；报告"这次实验是第 N 次尝试" |
| 随机性控制 | 所有含随机的环节固定 `seed`（VectorBT `from_random_signals(seed=...)`） |
| 实验可复现 | 每次实验记录：数据版本 + 因子版本 + 参数 + 代码 commit + 随机种子（MLflow + SQLite） |
| 成本压力测试 | 佣金/滑点 ×1.5、×2 下的收益表现 |

---

## 12. 可视化架构建议

### 12.1 三层架构（只写第三层里的"组装"）

```
① 指标计算层（现成库）   QuantStats / alphalens-reloaded / Qlib contrib.report / VectorBT returns
        ↓ 产出：metrics dict + 标准化 DataFrame（净值/回撤/IC/分层收益/参数网格）
② 图表层（现成库）      Plotly（交互）+ VectorBT plotting（回测图）+ matplotlib（静态备选、Qlib 兼容）
        ↓ 产出：Figure 对象 / HTML
③ 展示与归档层（自研薄） Jupyter（研究态）+ Streamlit（看板）+ Report Builder（组装成单个 HTML/PDF 存进实验目录）
```

### 12.2 你要求的每一类图表 → 现成实现来源（不要重复开发）

| 分类 | 图表 | 现成实现 | 需自研 |
| --- | --- | --- | --- |
| **回测** | 策略净值曲线 | VectorBT `pf.plot_value()` / Qlib `cumulative_return_graph` | ❌ 不需要 |
| | Benchmark 对比 | VectorBT 内置 `benchmark_returns`；Qlib 配置 `benchmark: SH000905` | ❌ |
| | Drawdown | VectorBT `pf.plot_drawdowns()` / `plot_underwater()` | ❌ |
| | Rolling Return / Rolling Sharpe | VectorBT `returns.vbt.returns.rolling_sharpe_ratio()` 等 `rolling_*` 系列 | ❌ |
| | 月度/年度收益热力图 | QuantStats tearsheet 自带 | ❌ |
| **交易** | 买卖点标注 | VectorBT `pf.plot_orders()` / `pf.plot_trades()` | ❌ |
| | 每笔交易明细 | VectorBT `pf.trades.records`（Records 对象，可直接存 Parquet） | ❌ |
| | 盈亏分布 / 胜率 / 盈亏比 | VectorBT `pf.trades.pnl.hist()` + stats（win_rate/profit_factor/expectancy） | ❌ |
| | 持仓时间分布 | VectorBT trades records 里有 `entry_idx/exit_idx` → 一行计算 + Plotly | ◐ 一行代码 |
| **因子** | IC / IC 分布 / IC 衰减 | **alphalens-reloaded**（`factor_information_coefficient` + `plot_ic_*`） | ❌ |
| | 因子分组收益 | alphalens `mean_return_by_quantile` + `plot_quantile_returns_bar` | ❌ |
| | 因子 Rank / 因子与未来收益关系 | alphalens `plot_quantile_average_cumulative_return` / `plot_ic_ts` | ❌ |
| | 因子稳定性（滚动 IC） | alphalens 滚动 IC + Plotly | ◐ 组合即可 |
| **参数** | 参数敏感性热力图 | **VectorBT `df.vbt.heatmap()` / `ts_heatmap()`** | ❌ |
| | 参数稳定区域识别 | 在热力图上叠加"夏普 > 阈值的连通区域" | ◐ 需要写（但很简单，scipy.ndimage.label 即可） |
| | 参数最优 vs 稳定对比 | 自研报告模板 | ◐ |
| **市场环境** | 牛/熊/震荡/高波动/低波动分段 | **无现成实现**：自己定义 regime（如指数 200 日均线上/下 + 波动率分位） | ✅ 需要写（约 50 行） |
| | 分市场环境的策略表现 | 把 regime 标签 join 到日收益上分组统计（Polars 一行） | ✅ 需要写（约 30 行） |

### 12.3 报告组装器（唯一值得自研的可视化代码）

**产出物**：一次实验 = 一个自包含 HTML（可离线打开、可发给自己、可归档），结构建议：

```
1. 实验头部：实验 ID / 时间 / 数据版本 / 因子版本 / 参数 / 代码 commit / 随机种子
2. 结论摘要：样本内 vs 样本外指标对照表 + "是否通过"判定（预设阈值，如 Sharpe>1.0 且 样本外衰减<30%）
3. 策略绩效：净值+Benchmark、回撤、Rolling Sharpe、月度热力图
4. 交易分析：买卖点、盈亏分布、胜率/盈亏比/持仓时间
5. 因子分析：IC/IC 衰减/分组收益/换手
6. 参数分析：参数热力图 + 稳定区域
7. 市场环境：分 regime 的表现
8. 风险与成本：佣金敏感性、换手率、最大回撤区间
9. 附录：完整参数 JSON（可复现）
```

**实现方式**：Plotly 出图 → `fig.to_html(full_html=False)` → 拼接 Jinja2 模板 → 单文件 HTML（`include_plotlyjs='cdn'` 或内联）→ 写入 `experiments/<id>/report.html` 并在 MLflow 里 `log_artifact("report.html")`。

---

## 13. AI Agent 架构建议

### 13.1 定位（最重要的一条）

```
┌──────────────────────────────────────────────────────┐
│  AI 研究助手（LLM）                                   │
│  职责：把"自然语言研究想法"翻译成"可执行、可复现的实验"  │
│  ❌ 不负责：下单、资金分配、参数以外的主观决策          │
└──────────────────────────────────────────────────────┘
                    ↓ 只能通过 MCP 工具调用
┌──────────────────────────────────────────────────────┐
│  平台能力层（你自研的工具契约）                        │
│  数据查询 / 因子计算 / 因子检验 / 回测 / 参数扫描 /      │
│  实验读写 / 报告生成 / 策略登记                        │
└──────────────────────────────────────────────────────┘
```

**核心原则：LLM 只被允许"调用已注册的工具"，不允许直接生成并执行任意回测代码。** 原因：①安全性（防止误删数据/误下单）；②可复现性（自由生成的代码无法审计）；③结果一致性（统一成本假设）。

### 13.2 目标闭环 → 工具映射

| 研究闭环步骤 | LLM 调用的工具（MCP tool） | 底层实现 |
| --- | --- | --- |
| 研究想法 → 假设 | （LLM 自身能力，产出结构化 JSON：假设/因子/目标变量/样本区间） | 提示词 + JSON schema |
| 定义因子 | `compute_factor(expr, params)` | **Qlib 表达式引擎**（安全 DSL，见 13.4）或向量化函数 |
| 定义目标变量 | `compute_label(method, horizon)` | VectorBT `labels`（FIXLB/TRENDLB/BOLX）或 quants-lab 的 Triple Barrier 思路 |
| 选择数据 | `list_datasets()` / `describe_schema(dataset)` | DuckDB 读 Parquet 元数据 |
| 设计实验 | `create_experiment(config)` | 写 YAML + 注册到 SQLite/MLflow |
| 调用回测 | `run_backtest(experiment_id)` / `run_param_sweep(...)` | Qlib `qrun` / VectorBT 扫描 |
| 生成统计结果 | `get_metrics(experiment_id)` | MLflow + artifacts |
| 生成可视化 | `render_report(experiment_id)` | Report Builder（第 12.3 节） |
| 解释结果 | （LLM 读结构化 metrics + 报告摘要） | 结构化 JSON，不要喂 raw parquet |
| 提出下一轮实验 | `propose_next_experiment(experiment_id)` | LLM + 历史实验对比（`compare_experiments`） |

### 13.3 推荐的工具清单（第一版 10 个就够）

```
list_datasets()                        # 有哪些数据、覆盖哪些年份
describe_schema(dataset)               # 字段/类型/覆盖率
query_data(dataset, fields, start, end, universe) -> 摘要统计（不返回全量！）
compute_factor(expr, name, params)     -> factor_id（写入 factor 仓库，返回统计摘要）
run_factor_test(factor_id, label, periods) -> IC/ICIR/分层/换手（alphalens）
run_backtest(factor_id, universe, cost_model, topk, ...) -> experiment_id
run_param_sweep(strategy_spec, grid, chunk_size) -> experiment_id + 热力图数据
get_experiment(experiment_id)          -> 配置 + 指标 + artifact 列表
compare_experiments(ids)               -> 对照表
render_report(experiment_id)           -> HTML 路径
```

### 13.4 关键设计：用"受限 DSL"替代"自由生成 Python"

**这是本报告最有价值的一条工程建议。**

- ❌ 危险做法：让 LLM 直接生成 Python 回测脚本并执行（不可审计、易写错、可能引入未来函数、可能删文件）。
- ✅ 安全做法：让 LLM 生成 **Qlib 风格的因子表达式**（`Mean($volume,5)/Mean($volume,20)-1`）和 **YAML 实验配置**，由你的平台解析执行。

原因：Qlib 的表达式引擎（`qlib/data/ops.py`）本身就是一套**受限、可枚举、可静态校验**的因子 DSL；它能做算术、时序算子（Ref/Mean/Std/Max/Min/Quantile）、横截面算子（Rank/CSRank）、条件运算，且天然与数据层对齐。**LLM 生成表达式 + 你校验表达式白名单 = 安全、可复现、可比较。**

同理，实验配置用 YAML（Qlib 已证明这条路可行），LLM 修改的是 YAML 字段而不是代码。

### 13.5 哪些开源项目最适合被 AI Agent 调用（排序）

| 排名 | 项目 | 为什么适合 LLM 调用 |
| --- | --- | --- |
| 1 | **Qlib** | 全流程由 **YAML 配置驱动**（`qrun config.yaml`），LLM 只需生成/修改结构化配置，不需要写代码；实验产物有固定命名规范，便于 LLM 读取结果 |
| 2 | **VectorBT** | **pandas-native API**，几行代码完成"指标→信号→组合→统计→画图"；`Portfolio.from_signals` 参数语义清晰，LLM 很容易正确调用（缺点是自由度高，需要工具封装） |
| 3 | **PyBroker** | 仓库自带 `skills/`（SKILL.md + openai.yaml + `.pyi` 类型桩 + 模板 + wiki 片段）和 `CLAUDE.md`，是"框架为 AI Agent 预置接口"的最佳范例 |
| 4 | **FastMCP** | 不是量化框架，而是把上述能力暴露成 MCP 工具的最短路径 |
| 5 | **RD-Agent（微软）** | 已经实现了"LLM 自动挖因子/调模型"的完整 loop 并与 Qlib 集成——**先读它的代码与论文，再决定自己做到什么程度** |

### 13.6 安全与风控边界（写死在代码里，不靠提示词）

| 边界 | 实现 |
| --- | --- |
| 禁止下单 | MCP server **不注册任何交易工具**；交易进程与研究进程物理隔离（不同 venv/用户/机器） |
| 禁止写原始数据 | `data/raw/**` 只读；LLM 只能写 `experiments/**` 与 `factor/**`（新因子） |
| 表达式白名单 | 只允许 `qlib/data/ops.py` 中的算子 + 数值常量；禁止 `eval/exec/import/os/subprocess` |
| 资源上限 | 单次回测限时/限内存（如 10 分钟、8GB），超限杀进程 |
| 人工确认点 | 参数扫描结果 → 进入候选池；候选 → 模拟盘；模拟盘 → 小资金实盘，三步都必须人工确认 |
| 审计日志 | 每次工具调用的入参/出参/耗时/LLM 会话 ID 落库（SQLite） |
| 过拟合护栏 | 平台强制记录"该数据集上累计实验次数"，并在报告中提示多重检验风险 |

---

## 14. 三套架构方案

### 14.1 方案 A：最简单 MVP（目标：2~4 周内跑通第一个实验）

```
┌─────────────────────────────────────────────────────────┐
│  Jupyter Notebook（唯一入口，AI/Codex 直接在这里工作）    │
└─────────────────────────────────────────────────────────┘
                 ↓
┌──────────────────────┐   ┌──────────────────────────────┐
│ AkShare 采集脚本      │→ │ Parquet 数据目录（行情+财务） │
└──────────────────────┘   └──────────────────────────────┘
                 ↓
┌─────────────────────────────────────────────────────────┐
│ VectorBT：指标 → 信号 → Portfolio.from_signals → 统计    │
│ QuantStats：tearsheet  ├─ Plotly：参数热力图             │
└─────────────────────────────────────────────────────────┘
                 ↓
        experiments/<日期_因子名>/  （config.yaml + metrics.json + report.html）
```

| 项 | 选型 |
| --- | --- |
| 组件数 | **5 个**（Python + Parquet + VectorBT + QuantStats + Plotly），DuckDB 可后加 |
| 数据库 | 无（或只在查询时用 DuckDB 读 Parquet） |
| 实验记录 | 手工 JSON/CSV（每实验一个目录） |
| 开发量 | 数据管道 3~5 天 + 规则层 3~5 天 + 实验模板 2~3 天 |
| 能回答的问题 | "放量后 5 日收益是否更高"这类单因子/单策略问题 |
| 不能做的 | 多因子合成、ML、严格 A股 规则（需补）、复现管理 |

**何时选 A**：你现在就想验证一个想法，且能接受"先粗糙、后补规范化"。

### 14.2 方案 B：推荐方案（目标：使用 3~5 年不需要推倒重来）★

```
                    ┌──────────────────────────────────────┐
                    │  AI 研究助手（Codex/Claude + Skills） │
                    │  只在"研究意图层"工作，禁止下单        │
                    └──────────────────────────────────────┘
                                    ↓ MCP 工具调用（FastMCP）
┌───────────────────────────────────────────────────────────────────────┐
│  平台能力层（自研薄层，约 2000~3000 行）                                │
│  ① 因子注册表（名称/表达式/参数/版本）  ② A股交易规则层（T+1/涨跌停/停牌/100股/税费）│
│  ③ 实验规范（config schema + artifact 命名）  ④ 成本模型  ⑤ 权重表中间格式 │
└───────────────────────────────────────────────────────────────────────┘
        ↓                                  ↓                    ↓
┌───────────────┐   ┌───────────────────────────┐   ┌────────────────────┐
│  数据层        │   │  回测/研究引擎层            │   │  分析与展示层       │
│  AkShare/Tushare│  │  Qlib（组合/ML/A股规则）    │   │  alphalens-reloaded │
│      ↓         │   │  VectorBT（参数扫描）       │   │  QuantStats          │
│  Parquet 仓库  │   │  └── Recorder → MLflow     │   │  Plotly + Streamlit  │
│  DuckDB 查询   │   │                            │   │  Report Builder(HTML)│
│  Polars 计算   │   │                            │   │                     │
└───────────────┘   └───────────────────────────┘   └────────────────────┘
                                    ↓
                    ┌──────────────────────────────┐
                    │  执行层（后置、物理隔离）       │
                    │  权重表(Parquet) → vnpy/QMT   │
                    │  模拟盘 → 小资金实盘 → 监控    │
                    └──────────────────────────────┘
```

| 项 | 选型 |
| --- | --- |
| 数据 | Parquet + DuckDB + Polars（元数据用 SQLite） |
| 回测 | Qlib（主干）+ VectorBT（扫描），共用一套成本与规则假设 |
| 因子 | 自建注册表 + alphalens-reloaded 检验 + Qlib Alpha158 表达式借鉴 |
| 实验管理 | MLflow（+ Qlib Recorder 的 artifact 命名规范） |
| 可视化 | Plotly + QuantStats + Qlib report + Streamlit |
| AI | FastMCP 工具层 + Skills 组织方式（抄 pybroker）+ 受限因子 DSL |
| 模拟盘/实盘 | vnpy / 券商 QMT（独立进程、独立数据库、独立账号权限） |
| 开发量 | 数据层 2~3 周；平台层 3~4 周；AI 层 1~2 周；**总计 6~10 周可跑通全闭环** |

**为什么这是推荐方案**：每个组件都有成熟开源实现，自研代码不到 3000 行，全部集中在"只有你能积累"的部分；3~5 年内数据量增长到几十 GB 也不需要换架构。

### 14.3 方案 C：高级方案（未来规模明显扩大时才考虑）

在方案 B 基础上叠加：

| 增补项 | 触发条件（不满足就不要做） |
| --- | --- |
| PostgreSQL（替代 SQLite 存元数据） | 多机/多人协作，或需要远程访问实验库 |
| ClickHouse / 时序库 | 分钟级/Tick 级数据（百 GB+），且 DuckDB 查询明显变慢 |
| Polars Lazy + Parquet 分区优化 + 对象存储(MinIO) | 数据超过单机内存、需要跨机共享 |
| Ray / Dask 分布式回测 | 单次参数扫描超过数小时且分块无法解决 |
| NautilusTrader | 需要分钟级/多市场、且"回测代码直接上实盘"成为刚需 |
| Kubernetes / Kafka / Redis Cluster | ⚠️ **个人量化研究几乎永远不需要**，这是典型的过度工程 |
| 自研 C++/Rust 撮合内核 | 只有做高频/做市才需要，与你的目标冲突 |

**方案 C 里"现在绝对不要做"的清单（明确）**：
❌ PostgreSQL ❌ ClickHouse ❌ Kafka ❌ Redis Cluster ❌ Kubernetes ❌ 微服务拆分 ❌ 自研回测引擎 ❌ 自研图表库 ❌ 自研实验管理系统 ❌ 分布式计算框架 ❌ 实时流式因子计算

---

## 15. 推荐长期架构（"如果这是我的项目，我会怎么搭"）

### 15.1 我会选择的方案

**方案 B，但按 A 的顺序落地**：先用 2~4 周做出方案 A（跑通第一个实验，建立信心与手感），随后在**不重写**的前提下演进到方案 B（把脚本抽成模块、把 JSON 换成 MLflow、把 VectorBT 扫描接上 Qlib 组合回测）。

### 15.2 我会怎么搭（具体到"先写哪个文件"）

```
第 1 步（第 1 周）  数据管道：AkShare → Parquet（行情 + 复权因子 + 停牌/涨跌停标记 + 股票列表含退市）
第 2 步（第 1 周）  查询层：DuckDB 视图 + Polars 读取函数（统一 get_price(universe, start, end)）
第 3 步（第 2 周）  A股规则层：T+1/涨跌停/停牌/100 股/费用（写成纯函数 + 单元测试，独立于任何引擎）
第 4 步（第 2 周）  因子层：因子注册表 + 首批 5~10 个因子（动量/波动/量能/换手/估值）
第 5 步（第 3 周）  回测：VectorBT 扫描打通（参数热力图）；同时用 Qlib 的 Exchange 规则做交叉校验
第 6 步（第 3 周）  实验规范：config.yaml schema + MLflow 记录 + 报告模板（HTML）
第 7 步（第 4 周）  AI 层：FastMCP 暴露 10 个工具 + skills 目录（抄 pybroker 组织方式）
第 8 步（第 2 个月）因子检验流水线（alphalens）+ 样本内外/Walk-forward + 参数稳定性
第 9 步（第 3 个月）多因子合成 + ML 模型（Qlib + LightGBM）
第 10 步（第 4 个月起）模拟盘（权重表 → vnpy/QMT），只在完整的样本外验证通过后启用
```

### 15.3 五条"我会坚持的架构原则"

1. **数据主权原则**：数据只以 Parquet 为准，任何框架（Qlib/VectorBT）都是"读数据的消费者"，绝不把数据迁进框架专有格式作为主存储。
2. **单一事实来源原则**：成本假设、交易日历、复权规则、股票池定义**只有一份**，所有引擎都从它读。
3. **研究/执行隔离原则**：研究环境**没有任何交易凭证**；执行环境只接受"目标权重表"，不接受研究代码。
4. **AI 只做研究不做执行原则**：LLM 的输出上限是"一份实验配置 + 一份报告"，不是"一个订单"。
5. **每加一个组件都要有理由原则**：引入任何新依赖前先回答"现有 5 个组件能不能做？"——本项目的第一性原理是**简单、稳定、可维护**，不是"看起来很专业"。

### 15.4 架构演进路线图

| 阶段 | 时间 | 目标 | 架构 |
| --- | --- | --- | --- |
| P0 打地基 | 0~1 个月 | 数据可信、单因子能跑通 | 方案 A（Parquet + VectorBT + QuantStats） |
| P1 建闭环 | 1~3 个月 | 实验可复现、因子可检验、报告可归档 | A → B（+ Qlib + MLflow + alphalens + 报告模板） |
| P2 上智能 | 3~6 个月 | AI 能生成并执行完整实验 | B（+ MCP 工具层 + 因子 DSL + skills） |
| P3 接执行 | 6~12 个月 | 模拟盘跑通、小资金验证 | B（+ vnpy/QMT 独立执行进程 + 风控开关） |
| P4 扩规模 | 1~3 年 | 因子库与策略库积累、分钟级研究 | B（按需引入方案 C 的个别组件） |

---

## 16. 最终技术栈

```text
语言：      Python 3.12（唯一主力语言）
            └─ 选它的原因：VectorBT v1.1.0 要求 >=3.11,<3.15；Qlib 要求 >=3.8；
              向量bt 的 numpy>=2.4 / pandas>=3.0 是较新约束，3.12 是当前最稳的交集
            └─ 环境管理：conda 或 uv（锁定 lock 文件，杜绝"半年后跑不起来"）
            └─ Rust/C++：只在必要时通过现成包使用（vectorbt-rust、hikyuu），不自己写

数据库：    DuckDB（分析查询，直接查 Parquet）+ SQLite（元数据/实验登记/任务状态）
            └─ 不引入 PostgreSQL / ClickHouse / MongoDB / Redis

数据格式：  Parquet（zstd 压缩，按年分区，单一事实来源）
            └─ 元数据用 SQLite；不把数据迁入 Qlib .bin 作为主存储

数据处理：  Polars（主力，LazyFrame + 多核）+ pandas（生态兼容层，仅在库要求时使用）
            + PyArrow（Parquet 读写）

数据采集：  AkShare（免费主力）+ Tushare Pro（付费财务数据）+ Baostock/pytdx（补充源）
            └─ 采集器输出统一落 Parquet，raw 层不可变

回测：      Qlib（主力：组合/多因子/ML/A股规则/嵌套执行）
            + VectorBT（参数扫描/快速研究/参数热力图）
            + 自研 A股规则适配层（T+1/涨跌停/停牌/100股/费用，两引擎共用）
            └─ 不引入 Backtrader / backtesting.py / zipline-reloaded

因子：      Qlib 表达式引擎（因子 DSL）+ 自建因子注册表与版本管理
            + alphalens-reloaded（IC/分层/换手/衰减检验）
            + Qlib Alpha158/Alpha360（现成因子清单，择优移植）

可视化：    Plotly（图表底层）+ QuantStats（绩效 tearsheet）
            + VectorBT plotting（回测图）+ Qlib contrib.report（因子/组合图组）
            + Streamlit（Web 看板）+ Jupyter（研究态）
            └─ 自研只做"报告组装器"（HTML 归档）

实验管理：  MLflow（params/metrics/tags/artifacts + UI）
            + Qlib Recorder 的 artifact 命名规范（pred/label/IC/组合报告）
            + SQLite（实验登记与策略生命周期）
            └─ 可选 DVC（数据版本）

Web：       Streamlit（第一版，研究看板与报告浏览）
            └─ 复杂交互再考虑 Dash，不引入前端框架

AI：        MCP Server（FastMCP）暴露 10 个研究工具
            + Skills 目录（照抄 pybroker 的 SKILL.md/openai.yaml/.pyi 组织方式）
            + 受限因子 DSL（LLM 只生成因子表达式与 YAML，不生成任意 Python）
            + 参考 microsoft/RD-Agent 的因子挖掘 loop
            └─ 禁止 AI 进入下单链路

模拟盘：    vnpy（国内通道最全，MIT）或券商官方 QMT/Ptrade 模拟
            └─ 独立进程 + 独立数据库 + 只读"目标权重表"，与研究环境物理隔离

实盘：      小资金 + 券商合规终端（QMT/Ptrade）+ vnpy 桥接
            └─ 必须有：单笔上限、单日亏损熔断、持仓对账、异常告警、人工确认开关
```

---

## 17. 开发优先级

### 17.1 P0（第 1 个月，必须做，做完才有资格谈研究）

| # | 任务 | 完成标准 |
| --- | --- | --- |
| P0-1 | 数据管道：AkShare → Parquet（行情 + 复权因子 + 停牌/涨跌停 + 股票列表含退市） | 能一键增量更新；数据质量断言全通过；已退市股票在列表中 |
| P0-2 | 统一查询层：DuckDB 视图 + `get_price(universe, start, end)` | 任意股票池查询 < 1 秒；查询接口只有一份 |
| P0-3 | A股交易规则层（纯函数 + 单元测试） | T+1/涨跌停/停牌/100 股/费用 5 类规则各有测试用例 |
| P0-4 | 成本模型与假设文档 | 佣金/印花税/过户费/滑点默认值可配置且被两个引擎共用 |
| P0-5 | 首个因子端到端实验（如"放量后 5 日收益"） | 从数据到净值曲线到 report.html 全流程可复现 |
| P0-6 | 实验目录规范（config.yaml + metrics.json + report.html + artifacts） | 每个实验都能凭 config 重跑出相同结果 |

### 17.2 P1（第 2~3 个月，建立研究闭环）

| # | 任务 |
| --- | --- |
| P1-1 | 接入 Qlib（先用 dump_bin 快速跑通，再写自定义 Provider） |
| P1-2 | 接入 MLflow，统一实验记录（与 P0-6 的目录规范对齐） |
| P1-3 | 因子注册表 + 10~20 个首批因子（动量/波动/量能/换手/估值/质量） |
| P1-4 | 接入 alphalens-reloaded，因子有效性检验流水线（IC/分层/换手/衰减） |
| P1-5 | 样本内/样本外切分 + Walk-forward（VectorBT Splitters） |
| P1-6 | 参数稳定性分析（热力图 + 稳定区域识别） |
| P1-7 | 双引擎一致性校验（20 个抽样策略交叉验证） |
| P1-8 | 报告组装器（单文件 HTML，归档到实验目录并上传 MLflow） |

### 17.3 P2（第 4~6 个月，AI 化与多因子）

| # | 任务 |
| --- | --- |
| P2-1 | FastMCP 工具层（10 个工具）+ 工具契约与权限边界 |
| P2-2 | 因子 DSL 白名单校验器（限制 LLM 生成范围） |
| P2-3 | skills 目录（照抄 pybroker 组织方式：SKILL.md + openapi.yaml + .pyi + 模板） |
| P2-4 | AI 研究闭环：假设 → 因子 → 实验 → 结果 → 下一轮建议 |
| P2-5 | 多因子合成（等权/IC 加权/最大化 ICIR）+ 中性化（行业/市值） |
| P2-6 | ML 模型（Qlib + LightGBM/线性），严格样本外评估 |
| P2-7 | 策略池与生命周期管理（研究→候选→模拟→实盘→淘汰） |
| P2-8 | Streamlit 看板（实验对比、因子库浏览、策略状态） |

### 17.4 P3（第 7~12 个月，走向执行）

| # | 任务 |
| --- | --- |
| P3-1 | 权重表标准格式 + 导出器（与研究层解耦） |
| P3-2 | vnpy/QMT 模拟盘接入（独立进程、只读权重表） |
| P3-3 | 风控开关（单笔上限/单日亏损熔断/黑名单/人工确认） |
| P3-4 | 每日自动对账（持仓/成交/净值 vs 研究侧预期） |
| P3-5 | 监控与告警（净值偏离、回撤超限、数据缺失、任务失败） |
| P3-6 | 小资金实盘（先单策略、后组合；先小仓位、后加仓） |
| P3-7 | 策略衰减监控与自动降级/暂停机制 |

### 17.5 ⚠️ 第一阶段绝对不要开发的东西（重要）

| 不要做 | 为什么 |
| --- | --- |
| ❌ 自研回测引擎 | Qlib/VectorBT 已覆盖，自研只会得到"更慢、更不准确"的结果 |
| ❌ 自研图表库/报告系统 | Plotly + QuantStats + Qlib report 已覆盖 90% |
| ❌ 自研实验管理系统 | MLflow 现成（Qlib Recorder 底层就是它） |
| ❌ 实盘/模拟盘对接 | 前 6 个月不要碰；没有经过完整样本外验证的策略不值得接实盘 |
| ❌ 高频/分钟级/Tick 级架构 | 目标不是高频；分钟级会带来 100 倍数据量与完全不同的问题 |
| ❌ PostgreSQL / ClickHouse / Kafka / K8s | 数据量在 GB 级，引入即过度工程 |
| ❌ 分布式回测 / 集群 | 单机分块足够；先优化分块策略 |
| ❌ Web 前端 / 用户系统 / 权限系统 | 你是唯一用户；Streamlit 足够 |
| ❌ 自研因子计算加速（Cython/Rust） | Polars/NumPy 够用；先解决数据质量与实验设计 |
| ❌ AI 自动下单 | 风险最高、收益最低；AI 的边界是"生成实验"，不是"做决策" |
| ❌ 一次接入 5 个框架 | 每多一个框架，成本假设与数据口径就多一份不一致风险 |

---

## 18. 最终结论

### 问题1：我们是否应该自己写回测引擎？

**不应该。明确不要写。**

- 扫描与快速研究：**VectorBT**（列=参数，一次算完，Numba/Rust 加速）
- 组合/多因子/ML/严格 A股 规则：**Qlib**（`Exchange` 已实现涨跌停、停牌、100 股一手、成交量约束、佣金/印花税结构）
- 你**唯一需要写**的是"**A股交易规则适配层**"（约 300~600 行纯函数 + 单元测试）：T+1、涨跌停不可成交、停牌、最小 100 股、ST/退市、复权、费用。这层不是"回测引擎"，而是"规则插件"，且必须被两个引擎共用。

### 问题2：我们是否应该自己写可视化？

**不应该写图表引擎；只写"报告组装器"。**

- 净值/回撤/买卖点/交易分析/参数热力图 → **VectorBT plotting 已全部实现**（`plot_value / plot_drawdowns / plot_orders / plot_trades / vbt.heatmap` 等）
- 绩效 tearsheet（月度热力图、滚动指标、回撤区间、Monte Carlo）→ **QuantStats 已实现**
- IC/分层收益/因子换手/衰减 → **alphalens-reloaded 已实现**
- 因子/组合的量化图组 → **Qlib `contrib/report` 已实现**
- 你需要写的只有：**把上述图表与指标拼成一个自包含 HTML，写进实验目录**（约 200 行）。

### 问题3：VectorBT 是否适合作为核心回测/研究引擎？

**适合作为"参数扫描与快速研究引擎"，不适合作为唯一核心。**

✅ 适合的理由：向量化参数扫描能力最强（这是你的高频需求）；`from_signals` 直接支持手续费/滑点/仓位/止损止盈/拒单/部分成交；`plot_*` 全家族 + 参数热力图；`rolling_*` 全套滚动指标；Splitters 支持 walk-forward；QuantStats 集成；labels 模块给 ML 生成标签。

⚠️ 不能独自承担的理由：
1. **许可证不是纯开源**（Apache-2.0 + Commons Clause，禁止"Sell the Software"）；
2. **完全不含 A股 规则**（涨跌停/停牌/T+1/100 股都要自己补，补错了回测就不真实）；
3. **不做实验管理**（没有 MLflow 那种记录/对比能力）；
4. **不覆盖财务因子**（数据与 PIT 对齐要自己解决）；
5. **版本约束激进**（v1.1.0 要求 Python>=3.11、numpy>=2.4.6、pandas>=3.0.3），若你的环境固定不了，会被迫退到旧版（0.26.x/0.27.x，API 有差异）；
6. **有走向 PRO（闭源付费）的倾向**，开源版特性增长会放缓。

**结论：VectorBT 用，但作为"第二引擎"用于扫描；主干回测交给 Qlib。**

### 问题4：Qlib 是否值得整体采用？

**值得采用，但不要"整体 100% 采用"。正确的姿势是"用骨架，不用数据格式"。**

| 采用 | 不采用 |
| --- | --- |
| ✅ Workflow YAML 的实验组织方式 | ❌ 把 `.bin` 作为主数据存储（数据主权留在 Parquet） |
| ✅ Recorder（基于 MLflow）的实验与产物规范 | ❌ RL / Meta-Learning / Online Serving / Orderbook 等机构特性 |
| ✅ Backtest 的 A股 规则与嵌套执行器 | ❌ 全盘照搬它的 10+ 层抽象到自己的代码里 |
| ✅ TopkDropoutStrategy 等选股策略 | ❌ 分布式/高频渲染相关配置 |
| ✅ Alpha158/360 的因子表达式清单 | |
| ✅ contrib/report 的图组、contrib/evaluate 的指标 | |
| ✅ 30+ 现成模型（LightGBM/XGBoost/CatBoost/PyTorch） | |

**注意其活跃度**：49k★ 但近一年仅 26 次提交、最新 release v0.9.7（2025-08）——它更像"微软发布后进入维护期的成熟框架"。因为它是 MIT + 代码质量高 + 你需要的能力已完备，**这个风险可接受**；但不要指望它快速修 bug 或新增特性。

### 问题5：Backtrader 是否需要加入？

**不需要。明确不加入。**

- 最后提交 **2023-04-19**，近一年 **0** 次提交，项目事实上停更；
- **GPL-3.0**，商业化传染风险高；
- 纯 Python 事件驱动，性能比 VectorBT 慢 2~3 个数量级；
- **没有国内实盘通道**（只有 IB/Oanda/VisualChart）。

**替代路径**：
- 需要"快速验证想法 + 参数优化" → VectorBT（或 backtesting.py 的思路，但注意 AGPL）
- 需要"事件驱动 + 回测到实盘一套代码" → **NautilusTrader**（LGPL-3.0，最活跃）
- 需要"国内模拟盘/实盘" → **vnpy + QMT/Ptrade**

**VectorBT + Backtrader 组合使用？不需要。** 两者数据模型不兼容，且成本/撮合假设无法对齐；正确的"组合"是**共享同一份中间产物**（信号/权重 Parquet），而不是互相调用。

### 问题6：本地数据仓库应该怎么做？

**Parquet + DuckDB + SQLite + Polars，四件套，不要更多。**

```
DuckDB（查询）──直接读──→ Parquet（事实存储，按年分区，zstd）
Polars（计算）──读写──→  同上
SQLite（元数据：实验登记/任务状态/策略清单/配置快照）
```

落地要点（按重要性排序）：
1. **分层目录**：`raw`（不可变原始）→ `market`（清洗行情 + 复权因子 + 停牌/涨跌停标记）→ `financial`（含 `ann_date` 公告日）→ `index`（指数/行业）→ `factor`（因子面板）
2. **复权因子单独存**，查询时计算复权价，不要存多套复权后的价格
3. **财务数据必须存 `ann_date`**，用 DuckDB `ASOF JOIN` 实现 PIT，否则就是未来函数
4. **股票列表必须包含退市股票**（否则存在生存者偏差，收益被系统性高估）
5. **交易日历统一用上交所日历**，所有数据与之对齐，缺失明确标记而非静默 ffill
6. **数据质量断言**每日执行（OHLC 关系、成交量非负、涨跌幅上限、日期唯一）

**不要**：PostgreSQL、ClickHouse、MongoDB、Redis、Kafka（个人日线研究用不到；等数据上到百 GB 再说）。

### 问题7：AI 应该放在哪一层？

**放在"研究意图层 + 实验编排层"，绝对不要进入"执行决策层"。**

```
✅ 允许：研究想法 → 假设 → 因子定义 → 实验配置 → 调用回测 → 统计与可视化 → 解释结果 → 下一轮建议
❌ 禁止：下单、调仓、资金分配、修改风控参数、访问交易凭证
```

实现要点：
1. **接口层**：FastMCP 暴露约 10 个工具（list_datasets / query_data / compute_factor / run_factor_test / run_backtest / run_param_sweep / get_experiment / compare_experiments / render_report / propose_next_experiment）
2. **让 LLM 生成"配置与表达式"而不是"任意代码"**：用 Qlib 的因子表达式作为受限 DSL（`Mean($volume,5)/Mean($volume,20)-1`），实验用 YAML——安全、可静态校验、可复现
3. **结果结构化**：给 LLM 喂 metrics JSON 与报告摘要，不要喂原始 Parquet
4. **参考实现**：pybroker 的 `skills/` 目录组织方式（SKILL.md + openai.yaml + `.pyi` 类型桩 + 模板 + wiki 片段）；微软 RD-Agent 的因子挖掘 loop
5. **审计**：每次工具调用入参/出参/会话 ID 落 SQLite；并强制记录"该数据集上累计实验次数"以提示多重检验风险

### 问题8：最终哪些模块是我们的核心资产？

**只有下面 5 项是"资产"，其余都是"可替换的零件"：**

| # | 核心资产 | 为什么是资产 |
| --- | --- | --- |
| 1 | **A股数据仓库与数据管道** | 决定研究结论的正确性；别人无法复用，且越用越值钱（历史数据、清洗规则、存活偏差修正） |
| 2 | **A股交易规则适配层与成本模型** | 决定回测是否可信；是所有策略的共同基准 |
| 3 | **因子库 + 因子检验流水线** | 因子是可积累、可组合、可复用的研究资产；检验流程保证质量 |
| 4 | **实验规范与策略生命周期管理** | 保证"第 100 次实验"比"第 1 次实验"更聪明而非更混乱 |
| 5 | **AI 研究助手的工具契约与 skills** | 让你的研究效率随时间复利增长（每次调用的经验都能沉淀成工具与模板） |

**不是资产的**：回测引擎（Qlib/VectorBT）、图表库（Plotly）、实验管理系统（MLflow）、数据库（DuckDB）——这些都是**可替换零件**，不要在上面投入自研成本。

### 最终一句话

> **站在 Qlib 与 VectorBT 的肩膀上，把全部自研精力投入到"数据 + A股规则 + 因子 + 实验规范 + AI 工具层"这 5 件事上。第一版不要超过 5 个组件，第一天就开始跑第一个实验。**

---

# 附录

## 附录 A：License 汇总表（原文核对结果）

| 项目 | LICENSE 文件实际内容 | 是否 OSI 开源 | 个人研究自用 | 商业化限制要点 |
| --- | --- | --- | --- | --- |
| microsoft/qlib | MIT License (Copyright © Microsoft) | ✅ | 自由 | 保留版权声明即可 |
| polakowo/vectorbt | **Apache 2.0 with Commons Clause**（LICENSE.md 首段即 Commons Clause 条件） | ❌ | 自由 | 不得"Sell the Software"（含以该软件为主要价值来源的托管/咨询服务） |
| edtechre/pybroker | **Apache 2.0 with Commons Clause**（"Software: PyBroker"） | ❌ | 自由 | 同上 |
| mementum/backtrader | GNU GPL v3（Copyright 2015-2023） | ✅ | 自由 | 衍生作品须以 GPL 开放；SaaS 场景也需谨慎 |
| kernc/backtesting.py | GNU **AGPL** v3 | ✅ | 自由 | 最强传染性：网络提供服务也触发开源义务 |
| nautechsystems/nautilus_trader | GNU LGPL v3 | ✅ | 自由 | 可商用；修改 LGPL 部分需开放该部分并允许替换 |
| QuantConnect/Lean | Apache License 2.0 | ✅ | 自由 | 保留声明 |
| stefan-jansen/zipline-reloaded | Apache License 2.0 | ✅ | 自由 | 保留声明 |
| pmorissette/bt | MIT | ✅ | 自由 | 保留声明 |
| fasiondog/hikyuu | Apache License 2.0 | ✅ | 自由 | 保留声明；README 另有免责声明（仅供研究，不构成投资建议） |
| yutiansut/QUANTAXIS | MIT (© 2016-2021) | ✅ | 自由 | 保留声明 |
| zvtvz/zvt | MIT (© 2023-2026 zvtvz) | ✅ | 自由 | 保留声明 |
| vnpy/vnpy | MIT | ✅ | 自由 | 保留声明 |
| akfamily/akshare | MIT | ✅ | 自由 | **注意数据来源自身的合规性（见附录 B）** |
| waditu/tushare | BSD-3-Clause | ✅ | 自由 | SDK 开源；**数据服务为付费商业服务，受其服务条款约束** |
| stefan-jansen/alphalens-reloaded | Apache License 2.0 | ✅ | 自由 | 保留声明 |
| ranaroussi/quantstats | Apache License 2.0 | ✅ | 自由 | 保留声明 |
| duckdb/duckdb | MIT | ✅ | 自由 | 保留声明 |
| pola-rs/polars | MIT | ✅ | 自由 | 保留声明 |
| apache/arrow | Apache License 2.0 | ✅ | 自由 | 保留声明 |
| mlflow/mlflow | Apache License 2.0 | ✅ | 自由 | 保留声明 |
| iterative/dvc | Apache License 2.0 | ✅ | 自由 | 保留声明 |
| streamlit/streamlit | Apache License 2.0 | ✅ | 自由 | 保留声明 |
| plotly/plotly.py | MIT | ✅ | 自由 | 保留声明 |
| plotly/dash | MIT | ✅ | 自由 | 保留声明 |
| gradio-app/gradio | Apache License 2.0 | ✅ | 自由 | 保留声明 |
| jlowin/fastmcp | Apache License 2.0 | ✅ | 自由 | 保留声明 |

> ⚠️ **本表为依据仓库 LICENSE 原文的技术性整理，不构成法律意见。** 若你未来计划把成果商业化（卖信号、卖软件、提供托管服务），务必就 Commons Clause / GPL / AGPL 条款咨询专业人士。
>
> 📌 **对个人研究实验室的实际影响：几乎没有。** 你只是"自己用"这些库做研究和自己账户的交易，Commons Clause 与 GPL 的限制都不触发。

## 附录 B：数据来源与合规提示

| 数据源 | 性质 | 建议 |
| --- | --- | --- |
| **AkShare** | MIT 开源库，聚合公开数据接口 | ✅ 主力采集工具；注意其抓取的上游站点自身条款，控制请求频率 |
| **Tushare Pro** | 商业数据服务（积分制/付费） | ✅ 财务数据的首选（质量与一致性最好）；遵守其服务协议与再分发限制 |
| **Baostock / pytdx** | 免费 / 通达信协议 | ✅ 补充源；注意稳定性和接口变更 |
| **同花顺 MCP / 其他第三方 MCP** | 第三方服务，条款需自行确认 | ⚠️ **使用前必须确认该服务的授权范围**：是否允许程序化访问、是否允许缓存与本地留存、是否允许再分发。**自用研究 ≠ 可以对外提供数据** |
| 券商官方接口（QMT/Ptrade） | 券商授权 | ✅ 实盘/模拟盘的正规通道，通常要求资产门槛 |

**三条实践原则**：
1. **数据落本地只为自己研究用**，不对外提供、不再分发、不公开原始数据。
2. **优先使用有明确授权的数据源**（券商接口、付费数据服务），把爬虫/第三方 MCP 当作补充而非根基。
3. **保留数据来源与获取时间的元数据**（`source`/`fetched_at` 字段），便于日后审计与替换。

## 附录 C：本次调研的方法与证据留存

**调研方式（可复现）**：
1. 通过 GitHub API 与 shields.io 徽章接口抓取仓库元数据（star/fork/license/last-commit/年度提交数/贡献者/最新 release）。
2. 抓取各仓库 `LICENSE` 与 `README` **原文**并逐条核对许可类型。
3. 通过 codeload 下载 **15 个仓库的完整源码包**并解压检查真实目录结构（而非只读 README）。
4. 对 VectorBT（v1.1.0 tag + master）、Qlib（main）、Backtrader、PyBroker、Hikyuu、ZVT、QUANTAXIS、QuantsLab 的关键模块做源码级核对（类名、函数签名、artifact 命名、配置字段）。

**证据文件位置（工作区内）**：

```
_research/
├── raw/signals.json          # 29 个项目的全部指标原始数据（star/license/活跃度/release）
├── raw/signals.tsv           # 同上，表格形式
├── raw/html/*.html           # 各项目 GitHub 主页存档
├── raw/feeds/*.atom          # 各项目提交流（用于活跃度计算）
├── raw/files/*/              # README / LICENSE / pyproject.toml / setup.py 原文
├── tmp/pypi_vectorbt.json    # PyPI 元数据（63 个版本、依赖、Python 版本要求）
└── repos/                    # 15 个已解压源码仓库
    ├── vectorbt_v110/        # VectorBT v1.1.0 完整源码（含 vectorbt/ 与 rust/）
    ├── qlib/                 # Qlib main 分支
    ├── backtrader/           
    ├── hikyuu/  quantaxis/  zvt/  vnpy/  nautilus/
    ├── backtesting_py/  pybroker/  quantstats/  alphalens_reloaded/
    └── quants_lab/  quantlab_sqyangx/  alpha_lab/
```

**关键数据快照（2026-09-14）**：

| 项目 | Star | 近一年提交 | 最后提交 | License |
| --- | --- | --- | --- | --- |
| microsoft/qlib | 49k | 26 | 2026-07-23 | MIT |
| polakowo/vectorbt | 9.1k | 147 | 2026-08-02 | Apache-2.0 + Commons Clause |
| mementum/backtrader | 23k | **0** | **2023-04-19** | GPL-3.0 |
| hummingbot/quants-lab | 297 | 活跃 | 2026 年内 | Apache-2.0 |
| nautechsystems/nautilus_trader | 29k | 5.4k | 2026-09-14 | LGPL-3.0 |
| fasiondog/hikyuu | 3.5k | 1.3k | 2026-09-11 | Apache-2.0 |
| yutiansut/QUANTAXIS | 11k | 29 | 2026-09-01 | MIT |
| zvtvz/zvt | 4.3k | 12 | 2026-07-01 | MIT |
| vnpy/vnpy | 45k | 54 | 2026-08-06 | MIT |
| akfamily/akshare | 23k | 374 | 2026-08-28 | MIT |
| mlflow/mlflow | 28k | 4.7k | 2026-09-14 | Apache-2.0 |

## 附录 D：需要你确认的三个问题

1. **`quant-lab` 具体指哪个仓库？** 本报告按 `hummingbot/quants-lab` 分析。如果你指的是 `sqyangx/quantlab`（A股 vibe trading）或其他，请告知，我重做第 6 节。
2. **数据源的实际可用性**：你提到的"同花顺 MCP"是否已确认授权与调用方式？这将直接决定数据管道 P0-1 的实现方式。
3. **运行环境约束**：本机 Python 版本、内存大小、是否需要 Windows 常驻运行？这会影响 VectorBT 版本选择（v1.1.0 需 Python ≥3.11 / numpy ≥2.4 / pandas ≥3.0）与数据分块策略。

---

*报告结束。所有结论均基于 2026-09-14 的仓库快照与源码实测；Star 数、活跃度会随时间变化，架构建议相对稳定。*
